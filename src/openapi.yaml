openapi: 3.0.3
info:
  title: ''
  version: 0.0.0
paths:
  /413.json:
    get:
      operationId: 413.json_retrieve
      tags:
        - 413.json
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          description: OK
  /500.json:
    get:
      operationId: 500.json_retrieve
      tags:
        - 500.json
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          description: OK
  /api/v1/bestandsdelen/{uuid}:
    put:
      operationId: bestandsdelen_update
      description: |-
        update:
        Upload een bestandsdeel
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - bestandsdelen
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BestandsDeelRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BestandsDeelRequest'
        required: true
      security:
        - JWT-Claims:
            - documenten.bijwerken
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BestandsDeel'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
  /api/v1/enkelvoudiginformatieobjecten:
    get:
      operationId: enkelvoudiginformatieobjecten_list
      description: |-
        Opvragen en bewerken van (ENKELVOUDIG) INFORMATIEOBJECTen (documenten).

        create:
        Maak een (ENKELVOUDIG) INFORMATIEOBJECT aan.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobjecttype` URL - de resource moet opgevraagd kunnen
          worden uit de catalogi API en de vorm van een INFORMATIEOBJECTTYPE hebben.
        - publicatie `informatieobjecttype` - `concept` moet `false` zijn

        list:
        Alle (ENKELVOUDIGe) INFORMATIEOBJECTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        De objecten bevatten metadata over de documenten en de downloadlink
        (`inhoud`) naar de binary data. Alleen de laatste versie van elk
        (ENKELVOUDIG) INFORMATIEOBJECT wordt getoond. Specifieke versies kunnen
        alleen

        retrieve:
        Een specifiek (ENKELVOUDIG) INFORMATIEOBJECT opvragen.

        Het object bevat metadata over het document en de downloadlink (`inhoud`)
        naar de binary data. Dit geeft standaard de laatste versie van het
        (ENKELVOUDIG) INFORMATIEOBJECT. Specifieke versies kunnen middels
        query-string parameters worden opgevraagd.

        update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT in zijn geheel bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        partial_update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT deels bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        destroy:
        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT.

        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT en alle bijbehorende versies,
        samen met alle gerelateerde resources binnen deze API. Dit is alleen mogelijk
        als er geen OBJECTINFORMATIEOBJECTen relateerd zijn aan het (ENKELVOUDIG)
        INFORMATIEOBJECT.

        **Gerelateerde resources**
        - GEBRUIKSRECHTen
        - audit trail regels

        download:
        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        lock:
        Vergrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Voert een "checkout" uit waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        vergrendeld wordt met een `lock` waarde. Alleen met deze waarde kan het
        (ENKELVOUDIG) INFORMATIEOBJECT bijgewerkt (`PUT`, `PATCH`) en weer
        ontgrendeld worden.

        unlock:
        Ontgrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Heft de "checkout" op waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        ontgrendeld wordt.
      parameters:
        - name: bronorganisatie
          required: false
          in: query
          description:
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie
            die het informatieobject heeft gecreëerd of heeft ontvangen en als eerste
            in een samenwerkingsketen heeft vastgelegd.
          schema:
            type: string
        - name: identificatie
          required: false
          in: query
          description:
            Een binnen een gegeven context ondubbelzinnige referentie naar
            het INFORMATIEOBJECT.
          schema:
            type: string
        - name: page
          required: false
          in: query
          description: Een pagina binnen de gepagineerde set resultaten.
          schema:
            type: integer
      tags:
        - enkelvoudiginformatieobjecten
      security:
        - JWT-Claims:
            - documenten.lezen
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedEnkelvoudigInformatieObjectList'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    post:
      operationId: enkelvoudiginformatieobjecten_create
      description: |-
        Opvragen en bewerken van (ENKELVOUDIG) INFORMATIEOBJECTen (documenten).

        create:
        Maak een (ENKELVOUDIG) INFORMATIEOBJECT aan.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobjecttype` URL - de resource moet opgevraagd kunnen
          worden uit de catalogi API en de vorm van een INFORMATIEOBJECTTYPE hebben.
        - publicatie `informatieobjecttype` - `concept` moet `false` zijn

        list:
        Alle (ENKELVOUDIGe) INFORMATIEOBJECTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        De objecten bevatten metadata over de documenten en de downloadlink
        (`inhoud`) naar de binary data. Alleen de laatste versie van elk
        (ENKELVOUDIG) INFORMATIEOBJECT wordt getoond. Specifieke versies kunnen
        alleen

        retrieve:
        Een specifiek (ENKELVOUDIG) INFORMATIEOBJECT opvragen.

        Het object bevat metadata over het document en de downloadlink (`inhoud`)
        naar de binary data. Dit geeft standaard de laatste versie van het
        (ENKELVOUDIG) INFORMATIEOBJECT. Specifieke versies kunnen middels
        query-string parameters worden opgevraagd.

        update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT in zijn geheel bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        partial_update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT deels bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        destroy:
        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT.

        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT en alle bijbehorende versies,
        samen met alle gerelateerde resources binnen deze API. Dit is alleen mogelijk
        als er geen OBJECTINFORMATIEOBJECTen relateerd zijn aan het (ENKELVOUDIG)
        INFORMATIEOBJECT.

        **Gerelateerde resources**
        - GEBRUIKSRECHTen
        - audit trail regels

        download:
        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        lock:
        Vergrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Voert een "checkout" uit waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        vergrendeld wordt met een `lock` waarde. Alleen met deze waarde kan het
        (ENKELVOUDIG) INFORMATIEOBJECT bijgewerkt (`PUT`, `PATCH`) en weer
        ontgrendeld worden.

        unlock:
        Ontgrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Heft de "checkout" op waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        ontgrendeld wordt.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
      tags:
        - enkelvoudiginformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnkelvoudigInformatieObjectCreateLockRequest'
        required: true
      security:
        - JWT-Claims:
            - documenten.aanmaken
      responses:
        '201':
          headers:
            Location:
              schema:
                type: string
                format: uri
              description: URL waar de resource leeft.
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnkelvoudigInformatieObjectCreateLock'
          description: Created
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
  /api/v1/enkelvoudiginformatieobjecten/{uuid}:
    get:
      operationId: enkelvoudiginformatieobjecten_retrieve
      description: |-
        Opvragen en bewerken van (ENKELVOUDIG) INFORMATIEOBJECTen (documenten).

        create:
        Maak een (ENKELVOUDIG) INFORMATIEOBJECT aan.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobjecttype` URL - de resource moet opgevraagd kunnen
          worden uit de catalogi API en de vorm van een INFORMATIEOBJECTTYPE hebben.
        - publicatie `informatieobjecttype` - `concept` moet `false` zijn

        list:
        Alle (ENKELVOUDIGe) INFORMATIEOBJECTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        De objecten bevatten metadata over de documenten en de downloadlink
        (`inhoud`) naar de binary data. Alleen de laatste versie van elk
        (ENKELVOUDIG) INFORMATIEOBJECT wordt getoond. Specifieke versies kunnen
        alleen

        retrieve:
        Een specifiek (ENKELVOUDIG) INFORMATIEOBJECT opvragen.

        Het object bevat metadata over het document en de downloadlink (`inhoud`)
        naar de binary data. Dit geeft standaard de laatste versie van het
        (ENKELVOUDIG) INFORMATIEOBJECT. Specifieke versies kunnen middels
        query-string parameters worden opgevraagd.

        update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT in zijn geheel bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        partial_update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT deels bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        destroy:
        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT.

        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT en alle bijbehorende versies,
        samen met alle gerelateerde resources binnen deze API. Dit is alleen mogelijk
        als er geen OBJECTINFORMATIEOBJECTen relateerd zijn aan het (ENKELVOUDIG)
        INFORMATIEOBJECT.

        **Gerelateerde resources**
        - GEBRUIKSRECHTen
        - audit trail regels

        download:
        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        lock:
        Vergrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Voert een "checkout" uit waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        vergrendeld wordt met een `lock` waarde. Alleen met deze waarde kan het
        (ENKELVOUDIG) INFORMATIEOBJECT bijgewerkt (`PUT`, `PATCH`) en weer
        ontgrendeld worden.

        unlock:
        Ontgrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Heft de "checkout" op waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        ontgrendeld wordt.
      parameters:
        - in: header
          name: If-None-Match
          schema:
            type: string
          description:
            Voer een voorwaardelijk verzoek uit. Deze header moet één of
            meerdere ETag-waardes bevatten van resources die de consumer gecached heeft.
            Indien de waarde van de ETag van de huidige resource voorkomt in deze set,
            dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match)
            voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: Eén ETag-waarde
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - enkelvoudiginformatieobjecten
      security:
        - JWT-Claims:
            - documenten.lezen
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
              description:
                De ETag berekend op de response body JSON. Indien twee
                resources exact dezelfde ETag hebben, dan zijn deze resources identiek
                aan elkaar. Je kan de ETag gebruiken om caching te implementeren.
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnkelvoudigInformatieObject'
          description: OK
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    put:
      operationId: enkelvoudiginformatieobjecten_update
      description: |-
        Opvragen en bewerken van (ENKELVOUDIG) INFORMATIEOBJECTen (documenten).

        create:
        Maak een (ENKELVOUDIG) INFORMATIEOBJECT aan.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobjecttype` URL - de resource moet opgevraagd kunnen
          worden uit de catalogi API en de vorm van een INFORMATIEOBJECTTYPE hebben.
        - publicatie `informatieobjecttype` - `concept` moet `false` zijn

        list:
        Alle (ENKELVOUDIGe) INFORMATIEOBJECTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        De objecten bevatten metadata over de documenten en de downloadlink
        (`inhoud`) naar de binary data. Alleen de laatste versie van elk
        (ENKELVOUDIG) INFORMATIEOBJECT wordt getoond. Specifieke versies kunnen
        alleen

        retrieve:
        Een specifiek (ENKELVOUDIG) INFORMATIEOBJECT opvragen.

        Het object bevat metadata over het document en de downloadlink (`inhoud`)
        naar de binary data. Dit geeft standaard de laatste versie van het
        (ENKELVOUDIG) INFORMATIEOBJECT. Specifieke versies kunnen middels
        query-string parameters worden opgevraagd.

        update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT in zijn geheel bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        partial_update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT deels bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        destroy:
        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT.

        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT en alle bijbehorende versies,
        samen met alle gerelateerde resources binnen deze API. Dit is alleen mogelijk
        als er geen OBJECTINFORMATIEOBJECTen relateerd zijn aan het (ENKELVOUDIG)
        INFORMATIEOBJECT.

        **Gerelateerde resources**
        - GEBRUIKSRECHTen
        - audit trail regels

        download:
        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        lock:
        Vergrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Voert een "checkout" uit waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        vergrendeld wordt met een `lock` waarde. Alleen met deze waarde kan het
        (ENKELVOUDIG) INFORMATIEOBJECT bijgewerkt (`PUT`, `PATCH`) en weer
        ontgrendeld worden.

        unlock:
        Ontgrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Heft de "checkout" op waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        ontgrendeld wordt.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - enkelvoudiginformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnkelvoudigInformatieObjectWithLockRequest'
        required: true
      security:
        - JWT-Claims:
            - (documenten.bijwerken | documenten.geforceerd-bijwerken)
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnkelvoudigInformatieObjectWithLock'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    patch:
      operationId: enkelvoudiginformatieobjecten_partial_update
      description: |-
        Opvragen en bewerken van (ENKELVOUDIG) INFORMATIEOBJECTen (documenten).

        create:
        Maak een (ENKELVOUDIG) INFORMATIEOBJECT aan.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobjecttype` URL - de resource moet opgevraagd kunnen
          worden uit de catalogi API en de vorm van een INFORMATIEOBJECTTYPE hebben.
        - publicatie `informatieobjecttype` - `concept` moet `false` zijn

        list:
        Alle (ENKELVOUDIGe) INFORMATIEOBJECTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        De objecten bevatten metadata over de documenten en de downloadlink
        (`inhoud`) naar de binary data. Alleen de laatste versie van elk
        (ENKELVOUDIG) INFORMATIEOBJECT wordt getoond. Specifieke versies kunnen
        alleen

        retrieve:
        Een specifiek (ENKELVOUDIG) INFORMATIEOBJECT opvragen.

        Het object bevat metadata over het document en de downloadlink (`inhoud`)
        naar de binary data. Dit geeft standaard de laatste versie van het
        (ENKELVOUDIG) INFORMATIEOBJECT. Specifieke versies kunnen middels
        query-string parameters worden opgevraagd.

        update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT in zijn geheel bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        partial_update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT deels bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        destroy:
        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT.

        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT en alle bijbehorende versies,
        samen met alle gerelateerde resources binnen deze API. Dit is alleen mogelijk
        als er geen OBJECTINFORMATIEOBJECTen relateerd zijn aan het (ENKELVOUDIG)
        INFORMATIEOBJECT.

        **Gerelateerde resources**
        - GEBRUIKSRECHTen
        - audit trail regels

        download:
        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        lock:
        Vergrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Voert een "checkout" uit waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        vergrendeld wordt met een `lock` waarde. Alleen met deze waarde kan het
        (ENKELVOUDIG) INFORMATIEOBJECT bijgewerkt (`PUT`, `PATCH`) en weer
        ontgrendeld worden.

        unlock:
        Ontgrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Heft de "checkout" op waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        ontgrendeld wordt.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - enkelvoudiginformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedEnkelvoudigInformatieObjectWithLockRequest'
      security:
        - JWT-Claims:
            - (documenten.bijwerken | documenten.geforceerd-bijwerken)
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnkelvoudigInformatieObjectWithLock'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    delete:
      operationId: enkelvoudiginformatieobjecten_destroy
      description: |-
        Opvragen en bewerken van (ENKELVOUDIG) INFORMATIEOBJECTen (documenten).

        create:
        Maak een (ENKELVOUDIG) INFORMATIEOBJECT aan.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobjecttype` URL - de resource moet opgevraagd kunnen
          worden uit de catalogi API en de vorm van een INFORMATIEOBJECTTYPE hebben.
        - publicatie `informatieobjecttype` - `concept` moet `false` zijn

        list:
        Alle (ENKELVOUDIGe) INFORMATIEOBJECTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        De objecten bevatten metadata over de documenten en de downloadlink
        (`inhoud`) naar de binary data. Alleen de laatste versie van elk
        (ENKELVOUDIG) INFORMATIEOBJECT wordt getoond. Specifieke versies kunnen
        alleen

        retrieve:
        Een specifiek (ENKELVOUDIG) INFORMATIEOBJECT opvragen.

        Het object bevat metadata over het document en de downloadlink (`inhoud`)
        naar de binary data. Dit geeft standaard de laatste versie van het
        (ENKELVOUDIG) INFORMATIEOBJECT. Specifieke versies kunnen middels
        query-string parameters worden opgevraagd.

        update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT in zijn geheel bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        partial_update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT deels bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        destroy:
        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT.

        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT en alle bijbehorende versies,
        samen met alle gerelateerde resources binnen deze API. Dit is alleen mogelijk
        als er geen OBJECTINFORMATIEOBJECTen relateerd zijn aan het (ENKELVOUDIG)
        INFORMATIEOBJECT.

        **Gerelateerde resources**
        - GEBRUIKSRECHTen
        - audit trail regels

        download:
        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        lock:
        Vergrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Voert een "checkout" uit waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        vergrendeld wordt met een `lock` waarde. Alleen met deze waarde kan het
        (ENKELVOUDIG) INFORMATIEOBJECT bijgewerkt (`PUT`, `PATCH`) en weer
        ontgrendeld worden.

        unlock:
        Ontgrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Heft de "checkout" op waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        ontgrendeld wordt.
      parameters:
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - enkelvoudiginformatieobjecten
      security:
        - JWT-Claims:
            - documenten.verwijderen
      responses:
        '204':
          description: No content
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
  /api/v1/enkelvoudiginformatieobjecten/{uuid}/download:
    get:
      operationId: enkelvoudiginformatieobjecten_download_retrieve
      description: |-
        Opvragen en bewerken van (ENKELVOUDIG) INFORMATIEOBJECTen (documenten).

        create:
        Maak een (ENKELVOUDIG) INFORMATIEOBJECT aan.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobjecttype` URL - de resource moet opgevraagd kunnen
          worden uit de catalogi API en de vorm van een INFORMATIEOBJECTTYPE hebben.
        - publicatie `informatieobjecttype` - `concept` moet `false` zijn

        list:
        Alle (ENKELVOUDIGe) INFORMATIEOBJECTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        De objecten bevatten metadata over de documenten en de downloadlink
        (`inhoud`) naar de binary data. Alleen de laatste versie van elk
        (ENKELVOUDIG) INFORMATIEOBJECT wordt getoond. Specifieke versies kunnen
        alleen

        retrieve:
        Een specifiek (ENKELVOUDIG) INFORMATIEOBJECT opvragen.

        Het object bevat metadata over het document en de downloadlink (`inhoud`)
        naar de binary data. Dit geeft standaard de laatste versie van het
        (ENKELVOUDIG) INFORMATIEOBJECT. Specifieke versies kunnen middels
        query-string parameters worden opgevraagd.

        update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT in zijn geheel bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        partial_update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT deels bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        destroy:
        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT.

        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT en alle bijbehorende versies,
        samen met alle gerelateerde resources binnen deze API. Dit is alleen mogelijk
        als er geen OBJECTINFORMATIEOBJECTen relateerd zijn aan het (ENKELVOUDIG)
        INFORMATIEOBJECT.

        **Gerelateerde resources**
        - GEBRUIKSRECHTen
        - audit trail regels

        download:
        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        lock:
        Vergrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Voert een "checkout" uit waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        vergrendeld wordt met een `lock` waarde. Alleen met deze waarde kan het
        (ENKELVOUDIG) INFORMATIEOBJECT bijgewerkt (`PUT`, `PATCH`) en weer
        ontgrendeld worden.

        unlock:
        Ontgrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Heft de "checkout" op waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        ontgrendeld wordt.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - enkelvoudiginformatieobjecten
      security:
        - JWT-Claims:
            - documenten.lezen
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/octet-stream:
              schema:
                $ref: '#/components/schemas/EnkelvoudigInformatieObject'
          description: OK
  /api/v1/enkelvoudiginformatieobjecten/{uuid}/lock:
    post:
      operationId: enkelvoudiginformatieobjecten_lock_create
      description: |-
        Opvragen en bewerken van (ENKELVOUDIG) INFORMATIEOBJECTen (documenten).

        create:
        Maak een (ENKELVOUDIG) INFORMATIEOBJECT aan.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobjecttype` URL - de resource moet opgevraagd kunnen
          worden uit de catalogi API en de vorm van een INFORMATIEOBJECTTYPE hebben.
        - publicatie `informatieobjecttype` - `concept` moet `false` zijn

        list:
        Alle (ENKELVOUDIGe) INFORMATIEOBJECTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        De objecten bevatten metadata over de documenten en de downloadlink
        (`inhoud`) naar de binary data. Alleen de laatste versie van elk
        (ENKELVOUDIG) INFORMATIEOBJECT wordt getoond. Specifieke versies kunnen
        alleen

        retrieve:
        Een specifiek (ENKELVOUDIG) INFORMATIEOBJECT opvragen.

        Het object bevat metadata over het document en de downloadlink (`inhoud`)
        naar de binary data. Dit geeft standaard de laatste versie van het
        (ENKELVOUDIG) INFORMATIEOBJECT. Specifieke versies kunnen middels
        query-string parameters worden opgevraagd.

        update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT in zijn geheel bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        partial_update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT deels bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        destroy:
        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT.

        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT en alle bijbehorende versies,
        samen met alle gerelateerde resources binnen deze API. Dit is alleen mogelijk
        als er geen OBJECTINFORMATIEOBJECTen relateerd zijn aan het (ENKELVOUDIG)
        INFORMATIEOBJECT.

        **Gerelateerde resources**
        - GEBRUIKSRECHTen
        - audit trail regels

        download:
        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        lock:
        Vergrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Voert een "checkout" uit waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        vergrendeld wordt met een `lock` waarde. Alleen met deze waarde kan het
        (ENKELVOUDIG) INFORMATIEOBJECT bijgewerkt (`PUT`, `PATCH`) en weer
        ontgrendeld worden.

        unlock:
        Ontgrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Heft de "checkout" op waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        ontgrendeld wordt.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - enkelvoudiginformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnkelvoudigInformatieObjectRequest'
        required: true
      security:
        - JWT-Claims:
            - documenten.lock
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnkelvoudigInformatieObject'
          description: OK
  /api/v1/enkelvoudiginformatieobjecten/{uuid}/unlock:
    post:
      operationId: enkelvoudiginformatieobjecten_unlock_create
      description: |-
        Opvragen en bewerken van (ENKELVOUDIG) INFORMATIEOBJECTen (documenten).

        create:
        Maak een (ENKELVOUDIG) INFORMATIEOBJECT aan.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobjecttype` URL - de resource moet opgevraagd kunnen
          worden uit de catalogi API en de vorm van een INFORMATIEOBJECTTYPE hebben.
        - publicatie `informatieobjecttype` - `concept` moet `false` zijn

        list:
        Alle (ENKELVOUDIGe) INFORMATIEOBJECTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        De objecten bevatten metadata over de documenten en de downloadlink
        (`inhoud`) naar de binary data. Alleen de laatste versie van elk
        (ENKELVOUDIG) INFORMATIEOBJECT wordt getoond. Specifieke versies kunnen
        alleen

        retrieve:
        Een specifiek (ENKELVOUDIG) INFORMATIEOBJECT opvragen.

        Het object bevat metadata over het document en de downloadlink (`inhoud`)
        naar de binary data. Dit geeft standaard de laatste versie van het
        (ENKELVOUDIG) INFORMATIEOBJECT. Specifieke versies kunnen middels
        query-string parameters worden opgevraagd.

        update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT in zijn geheel bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        partial_update:
        Werk een (ENKELVOUDIG) INFORMATIEOBJECT deels bij.

        Dit creëert altijd een nieuwe versie van het (ENKELVOUDIG) INFORMATIEOBJECT.

        **Er wordt gevalideerd op**
        - correcte `lock` waarde
        - het `informatieobjecttype` mag niet gewijzigd worden
        - status NIET `definitief`

        destroy:
        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT.

        Verwijder een (ENKELVOUDIG) INFORMATIEOBJECT en alle bijbehorende versies,
        samen met alle gerelateerde resources binnen deze API. Dit is alleen mogelijk
        als er geen OBJECTINFORMATIEOBJECTen relateerd zijn aan het (ENKELVOUDIG)
        INFORMATIEOBJECT.

        **Gerelateerde resources**
        - GEBRUIKSRECHTen
        - audit trail regels

        download:
        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        Download de binaire data van het (ENKELVOUDIG) INFORMATIEOBJECT.

        lock:
        Vergrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Voert een "checkout" uit waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        vergrendeld wordt met een `lock` waarde. Alleen met deze waarde kan het
        (ENKELVOUDIG) INFORMATIEOBJECT bijgewerkt (`PUT`, `PATCH`) en weer
        ontgrendeld worden.

        unlock:
        Ontgrendel een (ENKELVOUDIG) INFORMATIEOBJECT.

        Heft de "checkout" op waardoor het (ENKELVOUDIG) INFORMATIEOBJECT
        ontgrendeld wordt.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - enkelvoudiginformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnkelvoudigInformatieObjectRequest'
        required: true
      security:
        - JWT-Claims:
            - (documenten.lock | documenten.geforceerd-unlock)
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnkelvoudigInformatieObject'
          description: OK
  /api/v1/enkelvoudiginformatieobjecten/_zoek:
    post:
      operationId: enkelvoudiginformatieobjecten__zoek_create
      description: |-
        Voer een zoekopdracht uit op (ENKELVOUDIG) INFORMATIEOBJECTen .

        Zoeken/filteren gaat normaal via de `list` operatie, deze is echter
        niet geschikt voor zoekopdrachten met UUIDs.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
      tags:
        - enkelvoudiginformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnkelvoudigInformatieObjectRequest'
        required: true
      security:
        - JWT-Claims:
            - documenten.lezen
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnkelvoudigInformatieObject'
          description: OK
  /api/v1/gebruiksrechten:
    get:
      operationId: gebruiksrechten_list
      description: |-
        Opvragen en bewerken van GEBRUIKSRECHTen bij een INFORMATIEOBJECT.

        create:
        Maak een GEBRUIKSRECHT aan.

        Voeg GEBRUIKSRECHTen toe voor een INFORMATIEOBJECT.

        **Opmerkingen**
          - Het toevoegen van gebruiksrechten zorgt ervoor dat de
            `indicatieGebruiksrecht` op het informatieobject op `true` gezet wordt.

        list:
        Alle GEBRUIKSRECHTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke GEBRUIKSRECHT opvragen.

        Een specifieke GEBRUIKSRECHT opvragen.

        update:
        Werk een GEBRUIKSRECHT in zijn geheel bij.

        Werk een GEBRUIKSRECHT in zijn geheel bij.

        partial_update:
        Werk een GEBRUIKSRECHT relatie deels bij.

        Werk een GEBRUIKSRECHT relatie deels bij.

        destroy:
        Verwijder een GEBRUIKSRECHT.

        **Opmerkingen**
          - Indien het laatste GEBRUIKSRECHT van een INFORMATIEOBJECT verwijderd
            wordt, dan wordt de `indicatieGebruiksrecht` van het INFORMATIEOBJECT op
            `null` gezet.
      parameters:
        - name: einddatum__gt
          required: false
          in: query
          description:
            Einddatum van de periode waarin de gebruiksrechtvoorwaarden van
            toepassing zijn.
          schema:
            type: string
        - name: einddatum__gte
          required: false
          in: query
          description:
            Einddatum van de periode waarin de gebruiksrechtvoorwaarden van
            toepassing zijn.
          schema:
            type: string
        - name: einddatum__lt
          required: false
          in: query
          description:
            Einddatum van de periode waarin de gebruiksrechtvoorwaarden van
            toepassing zijn.
          schema:
            type: string
        - name: einddatum__lte
          required: false
          in: query
          description:
            Einddatum van de periode waarin de gebruiksrechtvoorwaarden van
            toepassing zijn.
          schema:
            type: string
        - name: informatieobject
          required: false
          in: query
          description: URL-referentie naar het INFORMATIEOBJECT.
          schema:
            type: string
          format: uri
        - name: startdatum__gt
          required: false
          in: query
          description:
            Begindatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn. Doorgaans is de datum van creatie van het informatieobject
            de startdatum.
          schema:
            type: string
        - name: startdatum__gte
          required: false
          in: query
          description:
            Begindatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn. Doorgaans is de datum van creatie van het informatieobject
            de startdatum.
          schema:
            type: string
        - name: startdatum__lt
          required: false
          in: query
          description:
            Begindatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn. Doorgaans is de datum van creatie van het informatieobject
            de startdatum.
          schema:
            type: string
        - name: startdatum__lte
          required: false
          in: query
          description:
            Begindatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn. Doorgaans is de datum van creatie van het informatieobject
            de startdatum.
          schema:
            type: string
      tags:
        - gebruiksrechten
      security:
        - JWT-Claims:
            - documenten.lezen
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Gebruiksrechten'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    post:
      operationId: gebruiksrechten_create
      description: |-
        Opvragen en bewerken van GEBRUIKSRECHTen bij een INFORMATIEOBJECT.

        create:
        Maak een GEBRUIKSRECHT aan.

        Voeg GEBRUIKSRECHTen toe voor een INFORMATIEOBJECT.

        **Opmerkingen**
          - Het toevoegen van gebruiksrechten zorgt ervoor dat de
            `indicatieGebruiksrecht` op het informatieobject op `true` gezet wordt.

        list:
        Alle GEBRUIKSRECHTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke GEBRUIKSRECHT opvragen.

        Een specifieke GEBRUIKSRECHT opvragen.

        update:
        Werk een GEBRUIKSRECHT in zijn geheel bij.

        Werk een GEBRUIKSRECHT in zijn geheel bij.

        partial_update:
        Werk een GEBRUIKSRECHT relatie deels bij.

        Werk een GEBRUIKSRECHT relatie deels bij.

        destroy:
        Verwijder een GEBRUIKSRECHT.

        **Opmerkingen**
          - Indien het laatste GEBRUIKSRECHT van een INFORMATIEOBJECT verwijderd
            wordt, dan wordt de `indicatieGebruiksrecht` van het INFORMATIEOBJECT op
            `null` gezet.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
      tags:
        - gebruiksrechten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GebruiksrechtenRequest'
        required: true
      security:
        - JWT-Claims:
            - documenten.aanmaken
      responses:
        '201':
          headers:
            Location:
              schema:
                type: string
                format: uri
              description: URL waar de resource leeft.
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Gebruiksrechten'
          description: Created
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
  /api/v1/gebruiksrechten/{uuid}:
    get:
      operationId: gebruiksrechten_retrieve
      description: |-
        Opvragen en bewerken van GEBRUIKSRECHTen bij een INFORMATIEOBJECT.

        create:
        Maak een GEBRUIKSRECHT aan.

        Voeg GEBRUIKSRECHTen toe voor een INFORMATIEOBJECT.

        **Opmerkingen**
          - Het toevoegen van gebruiksrechten zorgt ervoor dat de
            `indicatieGebruiksrecht` op het informatieobject op `true` gezet wordt.

        list:
        Alle GEBRUIKSRECHTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke GEBRUIKSRECHT opvragen.

        Een specifieke GEBRUIKSRECHT opvragen.

        update:
        Werk een GEBRUIKSRECHT in zijn geheel bij.

        Werk een GEBRUIKSRECHT in zijn geheel bij.

        partial_update:
        Werk een GEBRUIKSRECHT relatie deels bij.

        Werk een GEBRUIKSRECHT relatie deels bij.

        destroy:
        Verwijder een GEBRUIKSRECHT.

        **Opmerkingen**
          - Indien het laatste GEBRUIKSRECHT van een INFORMATIEOBJECT verwijderd
            wordt, dan wordt de `indicatieGebruiksrecht` van het INFORMATIEOBJECT op
            `null` gezet.
      parameters:
        - in: header
          name: If-None-Match
          schema:
            type: string
          description:
            Voer een voorwaardelijk verzoek uit. Deze header moet één of
            meerdere ETag-waardes bevatten van resources die de consumer gecached heeft.
            Indien de waarde van de ETag van de huidige resource voorkomt in deze set,
            dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match)
            voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: Eén ETag-waarde
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - gebruiksrechten
      security:
        - JWT-Claims:
            - documenten.lezen
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
              description:
                De ETag berekend op de response body JSON. Indien twee
                resources exact dezelfde ETag hebben, dan zijn deze resources identiek
                aan elkaar. Je kan de ETag gebruiken om caching te implementeren.
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Gebruiksrechten'
          description: OK
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    put:
      operationId: gebruiksrechten_update
      description: |-
        Opvragen en bewerken van GEBRUIKSRECHTen bij een INFORMATIEOBJECT.

        create:
        Maak een GEBRUIKSRECHT aan.

        Voeg GEBRUIKSRECHTen toe voor een INFORMATIEOBJECT.

        **Opmerkingen**
          - Het toevoegen van gebruiksrechten zorgt ervoor dat de
            `indicatieGebruiksrecht` op het informatieobject op `true` gezet wordt.

        list:
        Alle GEBRUIKSRECHTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke GEBRUIKSRECHT opvragen.

        Een specifieke GEBRUIKSRECHT opvragen.

        update:
        Werk een GEBRUIKSRECHT in zijn geheel bij.

        Werk een GEBRUIKSRECHT in zijn geheel bij.

        partial_update:
        Werk een GEBRUIKSRECHT relatie deels bij.

        Werk een GEBRUIKSRECHT relatie deels bij.

        destroy:
        Verwijder een GEBRUIKSRECHT.

        **Opmerkingen**
          - Indien het laatste GEBRUIKSRECHT van een INFORMATIEOBJECT verwijderd
            wordt, dan wordt de `indicatieGebruiksrecht` van het INFORMATIEOBJECT op
            `null` gezet.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - gebruiksrechten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GebruiksrechtenRequest'
        required: true
      security:
        - JWT-Claims:
            - documenten.bijwerken
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Gebruiksrechten'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    patch:
      operationId: gebruiksrechten_partial_update
      description: |-
        Opvragen en bewerken van GEBRUIKSRECHTen bij een INFORMATIEOBJECT.

        create:
        Maak een GEBRUIKSRECHT aan.

        Voeg GEBRUIKSRECHTen toe voor een INFORMATIEOBJECT.

        **Opmerkingen**
          - Het toevoegen van gebruiksrechten zorgt ervoor dat de
            `indicatieGebruiksrecht` op het informatieobject op `true` gezet wordt.

        list:
        Alle GEBRUIKSRECHTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke GEBRUIKSRECHT opvragen.

        Een specifieke GEBRUIKSRECHT opvragen.

        update:
        Werk een GEBRUIKSRECHT in zijn geheel bij.

        Werk een GEBRUIKSRECHT in zijn geheel bij.

        partial_update:
        Werk een GEBRUIKSRECHT relatie deels bij.

        Werk een GEBRUIKSRECHT relatie deels bij.

        destroy:
        Verwijder een GEBRUIKSRECHT.

        **Opmerkingen**
          - Indien het laatste GEBRUIKSRECHT van een INFORMATIEOBJECT verwijderd
            wordt, dan wordt de `indicatieGebruiksrecht` van het INFORMATIEOBJECT op
            `null` gezet.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - gebruiksrechten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedGebruiksrechtenRequest'
      security:
        - JWT-Claims:
            - documenten.bijwerken
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Gebruiksrechten'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    delete:
      operationId: gebruiksrechten_destroy
      description: |-
        Opvragen en bewerken van GEBRUIKSRECHTen bij een INFORMATIEOBJECT.

        create:
        Maak een GEBRUIKSRECHT aan.

        Voeg GEBRUIKSRECHTen toe voor een INFORMATIEOBJECT.

        **Opmerkingen**
          - Het toevoegen van gebruiksrechten zorgt ervoor dat de
            `indicatieGebruiksrecht` op het informatieobject op `true` gezet wordt.

        list:
        Alle GEBRUIKSRECHTen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke GEBRUIKSRECHT opvragen.

        Een specifieke GEBRUIKSRECHT opvragen.

        update:
        Werk een GEBRUIKSRECHT in zijn geheel bij.

        Werk een GEBRUIKSRECHT in zijn geheel bij.

        partial_update:
        Werk een GEBRUIKSRECHT relatie deels bij.

        Werk een GEBRUIKSRECHT relatie deels bij.

        destroy:
        Verwijder een GEBRUIKSRECHT.

        **Opmerkingen**
          - Indien het laatste GEBRUIKSRECHT van een INFORMATIEOBJECT verwijderd
            wordt, dan wordt de `indicatieGebruiksrecht` van het INFORMATIEOBJECT op
            `null` gezet.
      parameters:
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - gebruiksrechten
      security:
        - JWT-Claims:
            - documenten.verwijderen
      responses:
        '204':
          description: No content
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
  /api/v1/objectinformatieobjecten:
    get:
      operationId: objectinformatieobjecten_list
      description: |-
        Opvragen en verwijderen van OBJECT-INFORMATIEOBJECT relaties.

        Het betreft een relatie tussen een willekeurig OBJECT, bijvoorbeeld een
        ZAAK in de Zaken API, en een INFORMATIEOBJECT.

        create:
        Maak een OBJECT-INFORMATIEOBJECT relatie aan.

        **LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.**

        Andere API's, zoals de Zaken API en de Besluiten API, gebruiken dit
        endpoint bij het synchroniseren van relaties.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobject` URL
        - de combinatie `informatieobject` en `object` moet uniek zijn
        - bestaan van `object` URL

        list:
        Alle OBJECT-INFORMATIEOBJECT relaties opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke OBJECT-INFORMATIEOBJECT relatie opvragen.

        Een specifieke OBJECT-INFORMATIEOBJECT relatie opvragen.

        destroy:
        Verwijder een OBJECT-INFORMATIEOBJECT relatie.

        **LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.**

        Andere API's, zoals de Zaken API en de Besluiten API, gebruiken dit
        endpoint bij het synchroniseren van relaties.
      parameters:
        - name: informatieobject
          required: false
          in: query
          description: URL-referentie naar het INFORMATIEOBJECT.
          schema:
            type: string
          format: uri
        - name: object
          required: false
          in: query
          description:
            URL-referentie naar het gerelateerde OBJECT (in deze of een andere
            API).
          schema:
            type: string
          format: uri
      tags:
        - objectinformatieobjecten
      security:
        - JWT-Claims:
            - documenten.lezen
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ObjectInformatieObject'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    post:
      operationId: objectinformatieobjecten_create
      description: |-
        Opvragen en verwijderen van OBJECT-INFORMATIEOBJECT relaties.

        Het betreft een relatie tussen een willekeurig OBJECT, bijvoorbeeld een
        ZAAK in de Zaken API, en een INFORMATIEOBJECT.

        create:
        Maak een OBJECT-INFORMATIEOBJECT relatie aan.

        **LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.**

        Andere API's, zoals de Zaken API en de Besluiten API, gebruiken dit
        endpoint bij het synchroniseren van relaties.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobject` URL
        - de combinatie `informatieobject` en `object` moet uniek zijn
        - bestaan van `object` URL

        list:
        Alle OBJECT-INFORMATIEOBJECT relaties opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke OBJECT-INFORMATIEOBJECT relatie opvragen.

        Een specifieke OBJECT-INFORMATIEOBJECT relatie opvragen.

        destroy:
        Verwijder een OBJECT-INFORMATIEOBJECT relatie.

        **LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.**

        Andere API's, zoals de Zaken API en de Besluiten API, gebruiken dit
        endpoint bij het synchroniseren van relaties.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
      tags:
        - objectinformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ObjectInformatieObjectRequest'
        required: true
      security:
        - JWT-Claims:
            - documenten.aanmaken
      responses:
        '201':
          headers:
            Location:
              schema:
                type: string
                format: uri
              description: URL waar de resource leeft.
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectInformatieObject'
          description: Created
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
  /api/v1/objectinformatieobjecten/{uuid}:
    get:
      operationId: objectinformatieobjecten_retrieve
      description: |-
        Opvragen en verwijderen van OBJECT-INFORMATIEOBJECT relaties.

        Het betreft een relatie tussen een willekeurig OBJECT, bijvoorbeeld een
        ZAAK in de Zaken API, en een INFORMATIEOBJECT.

        create:
        Maak een OBJECT-INFORMATIEOBJECT relatie aan.

        **LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.**

        Andere API's, zoals de Zaken API en de Besluiten API, gebruiken dit
        endpoint bij het synchroniseren van relaties.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobject` URL
        - de combinatie `informatieobject` en `object` moet uniek zijn
        - bestaan van `object` URL

        list:
        Alle OBJECT-INFORMATIEOBJECT relaties opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke OBJECT-INFORMATIEOBJECT relatie opvragen.

        Een specifieke OBJECT-INFORMATIEOBJECT relatie opvragen.

        destroy:
        Verwijder een OBJECT-INFORMATIEOBJECT relatie.

        **LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.**

        Andere API's, zoals de Zaken API en de Besluiten API, gebruiken dit
        endpoint bij het synchroniseren van relaties.
      parameters:
        - in: header
          name: If-None-Match
          schema:
            type: string
          description:
            Voer een voorwaardelijk verzoek uit. Deze header moet één of
            meerdere ETag-waardes bevatten van resources die de consumer gecached heeft.
            Indien de waarde van de ETag van de huidige resource voorkomt in deze set,
            dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match)
            voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: Eén ETag-waarde
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - objectinformatieobjecten
      security:
        - JWT-Claims:
            - documenten.lezen
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
              description:
                De ETag berekend op de response body JSON. Indien twee
                resources exact dezelfde ETag hebben, dan zijn deze resources identiek
                aan elkaar. Je kan de ETag gebruiken om caching te implementeren.
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectInformatieObject'
          description: OK
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    delete:
      operationId: objectinformatieobjecten_destroy
      description: |-
        Opvragen en verwijderen van OBJECT-INFORMATIEOBJECT relaties.

        Het betreft een relatie tussen een willekeurig OBJECT, bijvoorbeeld een
        ZAAK in de Zaken API, en een INFORMATIEOBJECT.

        create:
        Maak een OBJECT-INFORMATIEOBJECT relatie aan.

        **LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.**

        Andere API's, zoals de Zaken API en de Besluiten API, gebruiken dit
        endpoint bij het synchroniseren van relaties.

        **Er wordt gevalideerd op**
        - geldigheid `informatieobject` URL
        - de combinatie `informatieobject` en `object` moet uniek zijn
        - bestaan van `object` URL

        list:
        Alle OBJECT-INFORMATIEOBJECT relaties opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke OBJECT-INFORMATIEOBJECT relatie opvragen.

        Een specifieke OBJECT-INFORMATIEOBJECT relatie opvragen.

        destroy:
        Verwijder een OBJECT-INFORMATIEOBJECT relatie.

        **LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.**

        Andere API's, zoals de Zaken API en de Besluiten API, gebruiken dit
        endpoint bij het synchroniseren van relaties.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - objectinformatieobjecten
      security:
        - JWT-Claims:
            - documenten.verwijderen
      responses:
        '204':
          description: No content
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
  /api/v1/verzendingen:
    get:
      operationId: verzendingen_list
      description: |-
        Opvragen en bewerken van VERZENDINGen.

        create:
        Maak een VERZENDING aan.

        Voeg VERZENDINGen toe voor een INFORMATIEOBJECT en een BETROKKENE.

        list:
        Alle VERZENDINGen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke VERZENDING opvragen.

        Een specifieke VERZENDING opvragen.

        update:
        Werk een VERZENDING in zijn geheel bij.

        Werk een VERZENDING in zijn geheel bij.

        partial_update:
        Werk een VERZENDING relatie deels bij.

        Werk een VERZENDING relatie deels bij.

        destroy:
        Verwijder een VERZENDING.
      parameters:
        - name: aardRelatie
          required: false
          in: query
          description:
            Omschrijving van de aard van de relatie van de BETROKKENE tot
            het INFORMATIEOBJECT.
          schema:
            type: string
            enum:
              - afzender
              - geadresseerde
        - name: betrokkene
          required: false
          in: query
          description:
            URL-referentie naar de betrokkene waarvan het informatieobject
            is ontvangen of waaraan dit is verzonden.
          schema:
            type: string
          format: uri
        - name: informatieobject
          required: false
          in: query
          description:
            URL-referentie naar het informatieobject dat is ontvangen of
            verzonden.
          schema:
            type: string
          format: uri
        - name: page
          required: false
          in: query
          description: Een pagina binnen de gepagineerde set resultaten.
          schema:
            type: integer
      tags:
        - verzendingen
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedVerzendingList'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    post:
      operationId: verzendingen_create
      description: |-
        Opvragen en bewerken van VERZENDINGen.

        create:
        Maak een VERZENDING aan.

        Voeg VERZENDINGen toe voor een INFORMATIEOBJECT en een BETROKKENE.

        list:
        Alle VERZENDINGen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke VERZENDING opvragen.

        Een specifieke VERZENDING opvragen.

        update:
        Werk een VERZENDING in zijn geheel bij.

        Werk een VERZENDING in zijn geheel bij.

        partial_update:
        Werk een VERZENDING relatie deels bij.

        Werk een VERZENDING relatie deels bij.

        destroy:
        Verwijder een VERZENDING.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
      tags:
        - verzendingen
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerzendingRequest'
        required: true
      responses:
        '201':
          headers:
            Location:
              schema:
                type: string
                format: uri
              description: URL waar de resource leeft.
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Verzending'
          description: Created
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
  /api/v1/verzendingen/{uuid}:
    get:
      operationId: verzendingen_retrieve
      description: |-
        Opvragen en bewerken van VERZENDINGen.

        create:
        Maak een VERZENDING aan.

        Voeg VERZENDINGen toe voor een INFORMATIEOBJECT en een BETROKKENE.

        list:
        Alle VERZENDINGen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke VERZENDING opvragen.

        Een specifieke VERZENDING opvragen.

        update:
        Werk een VERZENDING in zijn geheel bij.

        Werk een VERZENDING in zijn geheel bij.

        partial_update:
        Werk een VERZENDING relatie deels bij.

        Werk een VERZENDING relatie deels bij.

        destroy:
        Verwijder een VERZENDING.
      parameters:
        - in: header
          name: If-None-Match
          schema:
            type: string
          description:
            Voer een voorwaardelijk verzoek uit. Deze header moet één of
            meerdere ETag-waardes bevatten van resources die de consumer gecached heeft.
            Indien de waarde van de ETag van de huidige resource voorkomt in deze set,
            dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match)
            voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: Eén ETag-waarde
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - verzendingen
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
              description:
                De ETag berekend op de response body JSON. Indien twee
                resources exact dezelfde ETag hebben, dan zijn deze resources identiek
                aan elkaar. Je kan de ETag gebruiken om caching te implementeren.
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Verzending'
          description: OK
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    put:
      operationId: verzendingen_update
      description: |-
        Opvragen en bewerken van VERZENDINGen.

        create:
        Maak een VERZENDING aan.

        Voeg VERZENDINGen toe voor een INFORMATIEOBJECT en een BETROKKENE.

        list:
        Alle VERZENDINGen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke VERZENDING opvragen.

        Een specifieke VERZENDING opvragen.

        update:
        Werk een VERZENDING in zijn geheel bij.

        Werk een VERZENDING in zijn geheel bij.

        partial_update:
        Werk een VERZENDING relatie deels bij.

        Werk een VERZENDING relatie deels bij.

        destroy:
        Verwijder een VERZENDING.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - verzendingen
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerzendingRequest'
        required: true
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Verzending'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    patch:
      operationId: verzendingen_partial_update
      description: |-
        Opvragen en bewerken van VERZENDINGen.

        create:
        Maak een VERZENDING aan.

        Voeg VERZENDINGen toe voor een INFORMATIEOBJECT en een BETROKKENE.

        list:
        Alle VERZENDINGen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke VERZENDING opvragen.

        Een specifieke VERZENDING opvragen.

        update:
        Werk een VERZENDING in zijn geheel bij.

        Werk een VERZENDING in zijn geheel bij.

        partial_update:
        Werk een VERZENDING relatie deels bij.

        Werk een VERZENDING relatie deels bij.

        destroy:
        Verwijder een VERZENDING.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - verzendingen
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedVerzendingRequest'
      responses:
        '200':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Verzending'
          description: OK
        '400':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidatieFout'
          description: Bad request
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
    delete:
      operationId: verzendingen_destroy
      description: |-
        Opvragen en bewerken van VERZENDINGen.

        create:
        Maak een VERZENDING aan.

        Voeg VERZENDINGen toe voor een INFORMATIEOBJECT en een BETROKKENE.

        list:
        Alle VERZENDINGen opvragen.

        Deze lijst kan gefilterd wordt met query-string parameters.

        retrieve:
        Een specifieke VERZENDING opvragen.

        Een specifieke VERZENDING opvragen.

        update:
        Werk een VERZENDING in zijn geheel bij.

        Werk een VERZENDING in zijn geheel bij.

        partial_update:
        Werk een VERZENDING relatie deels bij.

        Werk een VERZENDING relatie deels bij.

        destroy:
        Verwijder een VERZENDING.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - verzendingen
      responses:
        '204':
          description: No content
        '401':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unauthorized
        '403':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Forbidden
        '404':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not found
        '406':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Not acceptable
        '409':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Conflict
        '410':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Gone
        '415':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Unsupported media type
        '429':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Too many requests
        '500':
          headers:
            API-version:
              schema:
                type: string
              description:
                'Geeft een specifieke API-versie aan in de context van
                een specifieke aanroep. Voorbeeld: 1.2.1.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fout'
          description: Internal server error
components:
  schemas:
    AardRelatieEnum:
      enum:
        - afzender
        - geadresseerde
      type: string
    AlgoritmeEnum:
      enum:
        - crc_16
        - crc_32
        - crc_64
        - fletcher_4
        - fletcher_8
        - fletcher_16
        - fletcher_32
        - hmac
        - md5
        - sha_1
        - sha_256
        - sha_512
        - sha_3
      type: string
    BestandsDeel:
      type: object
      properties:
        url:
          type: string
          format: uri
          readOnly: true
          description:
            URL-referentie naar dit object. Dit is de unieke identificatie
            en locatie van dit object.
          minLength: 1
          maxLength: 1000
        volgnummer:
          type: integer
          readOnly: true
          description: Een volgnummer dat de volgorde van de bestandsdelen aangeeft.
        omvang:
          type: integer
          readOnly: true
          description: De grootte van dit specifieke bestandsdeel.
        voltooid:
          type: boolean
          readOnly: true
          description:
            'Indicatie of dit bestandsdeel volledig is geupload. Dat wil
            zeggen: het aantal bytes dat staat genoemd bij grootte is daadwerkelijk
            ontvangen.'
      required:
        - omvang
        - url
        - volgnummer
        - voltooid
    BestandsDeelRequest:
      type: object
      properties:
        inhoud:
          type: string
          format: binary
          writeOnly: true
          description: De (binaire) bestandsinhoud van dit specifieke bestandsdeel.
        lock:
          type: string
          writeOnly: true
          minLength: 1
          description: Hash string, which represents id of the lock of related informatieobject
      required:
        - lock
    BinnenlandsCorrespondentieadresVerzending:
      type: object
      description:
        Het correspondentieadres, betreffende een adresseerbaar object,
        van de BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld in het
        ontvangen of verzonden INFORMATIEOBJECT indien dat afwijkt van het reguliere
        binnenlandse correspondentieadres van BETROKKENE.
      properties:
        huisletter:
          type: string
          description:
            Een door of namens het bevoegd gemeentelijk orgaan ten aanzien
            van een adresseerbaar object toegekende toevoeging aan een huisnummer
            in de vorm van een alfanumeriek teken.
          maxLength: 1
        huisnummer:
          type: integer
          maximum: 99999
          minimum: 1
          description:
            Een door of namens het bevoegd gemeentelijk orgaan ten aanzien
            van een adresseerbaar object toegekende nummering.
        huisnummerToevoeging:
          type: string
          description:
            Een door of namens het bevoegd gemeentelijk orgaan ten aanzien
            van een adresseerbaar object toegekende nadere toevoeging aan een huisnummer
            of een combinatie van huisnummer en huisletter.
          maxLength: 4
        naamOpenbareRuimte:
          type: string
          description:
            Een door het bevoegde gemeentelijke orgaan aan een GEMEENTELIJKE  OPENBARE
            RUIMTE toegekende benaming.
          maxLength: 80
        postcode:
          type: string
          description:
            De door TNT Post vastgestelde code behorende bij een bepaalde
            combinatie van een naam van een woonplaats, naam van een openbare ruimte
            en een huisnummer.
          maxLength: 6
        woonplaatsnaam:
          type: string
          description:
            De door het bevoegde gemeentelijke orgaan aan een WOONPLAATS
            toegekende benaming.
          maxLength: 80
      required:
        - huisnummer
        - naamOpenbareRuimte
        - woonplaatsnaam
      nullable: true
    BinnenlandsCorrespondentieadresVerzendingRequest:
      type: object
      description:
        Het correspondentieadres, betreffende een adresseerbaar object,
        van de BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld in het
        ontvangen of verzonden INFORMATIEOBJECT indien dat afwijkt van het reguliere
        binnenlandse correspondentieadres van BETROKKENE.
      properties:
        huisletter:
          type: string
          description:
            Een door of namens het bevoegd gemeentelijk orgaan ten aanzien
            van een adresseerbaar object toegekende toevoeging aan een huisnummer
            in de vorm van een alfanumeriek teken.
          maxLength: 1
        huisnummer:
          type: integer
          maximum: 99999
          minimum: 1
          description:
            Een door of namens het bevoegd gemeentelijk orgaan ten aanzien
            van een adresseerbaar object toegekende nummering.
        huisnummerToevoeging:
          type: string
          description:
            Een door of namens het bevoegd gemeentelijk orgaan ten aanzien
            van een adresseerbaar object toegekende nadere toevoeging aan een huisnummer
            of een combinatie van huisnummer en huisletter.
          maxLength: 4
        naamOpenbareRuimte:
          type: string
          minLength: 1
          description:
            Een door het bevoegde gemeentelijke orgaan aan een GEMEENTELIJKE  OPENBARE
            RUIMTE toegekende benaming.
          maxLength: 80
        postcode:
          type: string
          description:
            De door TNT Post vastgestelde code behorende bij een bepaalde
            combinatie van een naam van een woonplaats, naam van een openbare ruimte
            en een huisnummer.
          maxLength: 6
        woonplaatsnaam:
          type: string
          minLength: 1
          description:
            De door het bevoegde gemeentelijke orgaan aan een WOONPLAATS
            toegekende benaming.
          maxLength: 80
      required:
        - huisnummer
        - naamOpenbareRuimte
        - woonplaatsnaam
      nullable: true
    BlankEnum:
      enum:
        - ''
    BuitenlandsCorrespondentieadresVerzending:
      type: object
      description:
        De gegevens van het adres in het buitenland van BETROKKENE, zijnde
        afzender of geadresseerde, zoals vermeld in het ontvangen of verzonden INFORMATIEOBJECT
        en dat afwijkt van de reguliere correspondentiegegevens van BETROKKENE.
      properties:
        adresBuitenland1:
          type: string
          description:
            Het eerste deel dat behoort bij het afwijkend buitenlandse
            correspondentieadres van de betrokkene in zijn/haar rol bij de zaak.
          maxLength: 35
        adresBuitenland2:
          type: string
          description:
            Het tweede deel dat behoort bij het afwijkend buitenlandse
            correspondentieadres van de betrokkene in zijn/haar rol bij de zaak.
          maxLength: 35
        adresBuitenland3:
          type: string
          description:
            Het derde deel dat behoort bij het afwijkend buitenlandse correspondentieadres
            van de betrokkene in zijn/haar rol bij de zaak.
          maxLength: 35
        landPostadres:
          type: string
          format: uri
          description:
            Het LAND dat behoort bij het afwijkend buitenlandse correspondentieadres
            van de betrokkene in zijn/haar rol bij de zaak.
          maxLength: 200
      required:
        - adresBuitenland1
        - landPostadres
      nullable: true
    BuitenlandsCorrespondentieadresVerzendingRequest:
      type: object
      description:
        De gegevens van het adres in het buitenland van BETROKKENE, zijnde
        afzender of geadresseerde, zoals vermeld in het ontvangen of verzonden INFORMATIEOBJECT
        en dat afwijkt van de reguliere correspondentiegegevens van BETROKKENE.
      properties:
        adresBuitenland1:
          type: string
          minLength: 1
          description:
            Het eerste deel dat behoort bij het afwijkend buitenlandse
            correspondentieadres van de betrokkene in zijn/haar rol bij de zaak.
          maxLength: 35
        adresBuitenland2:
          type: string
          description:
            Het tweede deel dat behoort bij het afwijkend buitenlandse
            correspondentieadres van de betrokkene in zijn/haar rol bij de zaak.
          maxLength: 35
        adresBuitenland3:
          type: string
          description:
            Het derde deel dat behoort bij het afwijkend buitenlandse correspondentieadres
            van de betrokkene in zijn/haar rol bij de zaak.
          maxLength: 35
        landPostadres:
          type: string
          format: uri
          minLength: 1
          description:
            Het LAND dat behoort bij het afwijkend buitenlandse correspondentieadres
            van de betrokkene in zijn/haar rol bij de zaak.
          maxLength: 200
      required:
        - adresBuitenland1
        - landPostadres
      nullable: true
    BuitenlandsCorrespondentiepostadresVerzending:
      type: object
      description:
        De gegevens die tezamen een postbusadres of antwoordnummeradres
        vormen van BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld in
        het ontvangen of verzonden INFORMATIEOBJECT en dat afwijkt van de reguliere
        correspondentiegegevens van BETROKKENE.
      properties:
        postBusOfAntwoordnummer:
          type: integer
          maximum: 9999
          minimum: 1
          title: Postbus-of antwoordnummer
          description:
            De numerieke aanduiding zoals deze door de Nederlandse PTT
            is vastgesteld voor postbusadressen en antwoordnummeradressen.
        postadresPostcode:
          type: string
          description:
            De officiële Nederlandse PTT codering, bestaande uit een numerieke
            woonplaatscode en een alfabetische lettercode.
          maxLength: 6
        postadresType:
          allOf:
            - $ref: '#/components/schemas/PostadresTypeEnum'
          title: Postadrestype
          description: Aanduiding van het soort postadres.
        woonplaatsnaam:
          type: string
          description:
            De door het bevoegde gemeentelijke orgaan aan een WOONPLAATS
            toegekende benaming.
          maxLength: 80
      required:
        - postBusOfAntwoordnummer
        - postadresPostcode
        - postadresType
        - woonplaatsnaam
      nullable: true
    BuitenlandsCorrespondentiepostadresVerzendingRequest:
      type: object
      description:
        De gegevens die tezamen een postbusadres of antwoordnummeradres
        vormen van BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld in
        het ontvangen of verzonden INFORMATIEOBJECT en dat afwijkt van de reguliere
        correspondentiegegevens van BETROKKENE.
      properties:
        postBusOfAntwoordnummer:
          type: integer
          maximum: 9999
          minimum: 1
          title: Postbus-of antwoordnummer
          description:
            De numerieke aanduiding zoals deze door de Nederlandse PTT
            is vastgesteld voor postbusadressen en antwoordnummeradressen.
        postadresPostcode:
          type: string
          minLength: 1
          description:
            De officiële Nederlandse PTT codering, bestaande uit een numerieke
            woonplaatscode en een alfabetische lettercode.
          maxLength: 6
        postadresType:
          allOf:
            - $ref: '#/components/schemas/PostadresTypeEnum'
          title: Postadrestype
          description: Aanduiding van het soort postadres.
        woonplaatsnaam:
          type: string
          minLength: 1
          description:
            De door het bevoegde gemeentelijke orgaan aan een WOONPLAATS
            toegekende benaming.
          maxLength: 80
      required:
        - postBusOfAntwoordnummer
        - postadresPostcode
        - postadresType
        - woonplaatsnaam
      nullable: true
    EnkelvoudigInformatieObject:
      type: object
      description: Serializer for the EnkelvoudigInformatieObject model
      properties:
        url:
          type: string
          format: uri
          readOnly: true
          description:
            URL-referentie naar dit object. Dit is de unieke identificatie
            en locatie van dit object.
          minLength: 1
          maxLength: 1000
        identificatie:
          type: string
          description:
            Een binnen een gegeven context ondubbelzinnige referentie naar
            het INFORMATIEOBJECT.
          maxLength: 40
        bronorganisatie:
          type: string
          description:
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie
            die het informatieobject heeft gecreëerd of heeft ontvangen en als eerste
            in een samenwerkingsketen heeft vastgelegd.
          maxLength: 9
        creatiedatum:
          type: string
          format: date
          description: Een datum of een gebeurtenis in de levenscyclus van het INFORMATIEOBJECT.
        titel:
          type: string
          description: De naam waaronder het INFORMATIEOBJECT formeel bekend is.
          maxLength: 200
        vertrouwelijkheidaanduiding:
          description: |-
            Aanduiding van de mate waarin het INFORMATIEOBJECT voor de openbaarheid bestemd is.

            Uitleg bij mogelijke waarden:

            * `openbaar` - Openbaar
            * `beperkt_openbaar` - Beperkt openbaar
            * `intern` - Intern
            * `zaakvertrouwelijk` - Zaakvertrouwelijk
            * `vertrouwelijk` - Vertrouwelijk
            * `confidentieel` - Confidentieel
            * `geheim` - Geheim
            * `zeer_geheim` - Zeer geheim
          oneOf:
            - $ref: '#/components/schemas/VertrouwelijkheidaanduidingEnum'
            - $ref: '#/components/schemas/BlankEnum'
        auteur:
          type: string
          description:
            De persoon of organisatie die in de eerste plaats verantwoordelijk
            is voor het creëren van de inhoud van het INFORMATIEOBJECT.
          maxLength: 200
        status:
          description: |-
            Aanduiding van de stand van zaken van een INFORMATIEOBJECT. De waarden 'in bewerking' en 'ter vaststelling' komen niet voor als het attribuut `ontvangstdatum` van een waarde is voorzien. Wijziging van de Status in 'gearchiveerd' impliceert dat het informatieobject een duurzaam, niet-wijzigbaar Formaat dient te hebben.

            Uitleg bij mogelijke waarden:

            * `in_bewerking` - (In bewerking) Aan het informatieobject wordt nog gewerkt.
            * `ter_vaststelling` - (Ter vaststelling) Informatieobject gereed maar moet nog vastgesteld worden.
            * `definitief` - (Definitief) Informatieobject door bevoegd iets of iemand vastgesteld dan wel ontvangen.
            * `gearchiveerd` - (Gearchiveerd) Informatieobject duurzaam bewaarbaar gemaakt; een gearchiveerd informatie-element.
          oneOf:
            - $ref: '#/components/schemas/StatusEnum'
            - $ref: '#/components/schemas/BlankEnum'
        formaat:
          type: string
          description:
            'Het "Media Type" (voorheen "MIME type") voor de wijze waaropde
            inhoud van het INFORMATIEOBJECT is vastgelegd in een computerbestand.
            Voorbeeld: `application/msword`. Zie: https://www.iana.org/assignments/media-types/media-types.xhtml'
          maxLength: 255
        taal:
          type: string
          description:
            'Een ISO 639-2/B taalcode waarin de inhoud van het INFORMATIEOBJECT
            is vastgelegd. Voorbeeld: `nld`. Zie: https://www.iso.org/standard/4767.html'
          maxLength: 3
          minLength: 3
        versie:
          type: integer
          readOnly: true
          description:
            Het (automatische) versienummer van het INFORMATIEOBJECT. Deze
            begint bij 1 als het INFORMATIEOBJECT aangemaakt wordt.
        beginRegistratie:
          type: string
          format: date-time
          readOnly: true
          description:
            Een datumtijd in ISO8601 formaat waarop deze versie van het
            INFORMATIEOBJECT is aangemaakt of gewijzigd.
        bestandsnaam:
          type: string
          description:
            De naam van het fysieke bestand waarin de inhoud van het informatieobject
            is vastgelegd, inclusief extensie.
          maxLength: 255
        inhoud:
          type: string
          format: uri
          nullable: true
        bestandsomvang:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          description: Aantal bytes dat de inhoud van INFORMATIEOBJECT in beslag neemt.
        link:
          type: string
          format: uri
          description:
            De URL waarmee de inhoud van het INFORMATIEOBJECT op te vragen
            is.
          maxLength: 200
        beschrijving:
          type: string
          description: Een generieke beschrijving van de inhoud van het INFORMATIEOBJECT.
          maxLength: 1000
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt.
        indicatieGebruiksrecht:
          type: boolean
          nullable: true
          description:
            Indicatie of er beperkingen gelden aangaande het gebruik van
            het informatieobject anders dan raadpleging. Dit veld mag `null` zijn
            om aan te geven dat de indicatie nog niet bekend is. Als de indicatie
            gezet is, dan kan je de gebruiksrechten die van toepassing zijn raadplegen
            via de GEBRUIKSRECHTen resource.
        verschijningsvorm:
          type: string
          description: De essentiële opmaakaspecten van een INFORMATIEOBJECT.
        ondertekening:
          allOf:
            - $ref: '#/components/schemas/Ondertekening'
          nullable: true
          description:
            Aanduiding van de rechtskracht van een informatieobject. Mag
            niet van een waarde zijn voorzien als de `status` de waarde 'in bewerking'
            of 'ter vaststelling' heeft.
        integriteit:
          allOf:
            - $ref: '#/components/schemas/Integriteit'
          nullable: true
          description:
            Uitdrukking van mate van volledigheid en onbeschadigd zijn
            van digitaal bestand.
        informatieobjecttype:
          type: string
          format: uri
          description:
            URL-referentie naar het INFORMATIEOBJECTTYPE (in de Catalogi
            API).
          maxLength: 200
        locked:
          type: boolean
          readOnly: true
          description:
            Geeft aan of het document gelocked is. Alleen als een document
            gelocked is, mogen er aanpassingen gemaakt worden.
        bestandsdelen:
          type: array
          items:
            $ref: '#/components/schemas/BestandsDeel'
          readOnly: true
      required:
        - auteur
        - beginRegistratie
        - bestandsdelen
        - bronorganisatie
        - creatiedatum
        - informatieobjecttype
        - locked
        - taal
        - titel
        - url
        - versie
    EnkelvoudigInformatieObjectCreateLock:
      type: object
      description: |-
        This serializer class is used by EnkelvoudigInformatieObjectViewSet for
        create operation for large files
      properties:
        url:
          type: string
          format: uri
          readOnly: true
          description:
            URL-referentie naar dit object. Dit is de unieke identificatie
            en locatie van dit object.
          minLength: 1
          maxLength: 1000
        identificatie:
          type: string
          description:
            Een binnen een gegeven context ondubbelzinnige referentie naar
            het INFORMATIEOBJECT.
          maxLength: 40
        bronorganisatie:
          type: string
          description:
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie
            die het informatieobject heeft gecreëerd of heeft ontvangen en als eerste
            in een samenwerkingsketen heeft vastgelegd.
          maxLength: 9
        creatiedatum:
          type: string
          format: date
          description: Een datum of een gebeurtenis in de levenscyclus van het INFORMATIEOBJECT.
        titel:
          type: string
          description: De naam waaronder het INFORMATIEOBJECT formeel bekend is.
          maxLength: 200
        vertrouwelijkheidaanduiding:
          description: |-
            Aanduiding van de mate waarin het INFORMATIEOBJECT voor de openbaarheid bestemd is.

            Uitleg bij mogelijke waarden:

            * `openbaar` - Openbaar
            * `beperkt_openbaar` - Beperkt openbaar
            * `intern` - Intern
            * `zaakvertrouwelijk` - Zaakvertrouwelijk
            * `vertrouwelijk` - Vertrouwelijk
            * `confidentieel` - Confidentieel
            * `geheim` - Geheim
            * `zeer_geheim` - Zeer geheim
          oneOf:
            - $ref: '#/components/schemas/VertrouwelijkheidaanduidingEnum'
            - $ref: '#/components/schemas/BlankEnum'
        auteur:
          type: string
          description:
            De persoon of organisatie die in de eerste plaats verantwoordelijk
            is voor het creëren van de inhoud van het INFORMATIEOBJECT.
          maxLength: 200
        status:
          description: |-
            Aanduiding van de stand van zaken van een INFORMATIEOBJECT. De waarden 'in bewerking' en 'ter vaststelling' komen niet voor als het attribuut `ontvangstdatum` van een waarde is voorzien. Wijziging van de Status in 'gearchiveerd' impliceert dat het informatieobject een duurzaam, niet-wijzigbaar Formaat dient te hebben.

            Uitleg bij mogelijke waarden:

            * `in_bewerking` - (In bewerking) Aan het informatieobject wordt nog gewerkt.
            * `ter_vaststelling` - (Ter vaststelling) Informatieobject gereed maar moet nog vastgesteld worden.
            * `definitief` - (Definitief) Informatieobject door bevoegd iets of iemand vastgesteld dan wel ontvangen.
            * `gearchiveerd` - (Gearchiveerd) Informatieobject duurzaam bewaarbaar gemaakt; een gearchiveerd informatie-element.
          oneOf:
            - $ref: '#/components/schemas/StatusEnum'
            - $ref: '#/components/schemas/BlankEnum'
        formaat:
          type: string
          description:
            'Het "Media Type" (voorheen "MIME type") voor de wijze waaropde
            inhoud van het INFORMATIEOBJECT is vastgelegd in een computerbestand.
            Voorbeeld: `application/msword`. Zie: https://www.iana.org/assignments/media-types/media-types.xhtml'
          maxLength: 255
        taal:
          type: string
          description:
            'Een ISO 639-2/B taalcode waarin de inhoud van het INFORMATIEOBJECT
            is vastgelegd. Voorbeeld: `nld`. Zie: https://www.iso.org/standard/4767.html'
          maxLength: 3
          minLength: 3
        versie:
          type: integer
          readOnly: true
          description:
            Het (automatische) versienummer van het INFORMATIEOBJECT. Deze
            begint bij 1 als het INFORMATIEOBJECT aangemaakt wordt.
        beginRegistratie:
          type: string
          format: date-time
          readOnly: true
          description:
            Een datumtijd in ISO8601 formaat waarop deze versie van het
            INFORMATIEOBJECT is aangemaakt of gewijzigd.
        bestandsnaam:
          type: string
          description:
            De naam van het fysieke bestand waarin de inhoud van het informatieobject
            is vastgelegd, inclusief extensie.
          maxLength: 255
        inhoud:
          type: string
          format: uri
          nullable: true
        bestandsomvang:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          description: Aantal bytes dat de inhoud van INFORMATIEOBJECT in beslag neemt.
        link:
          type: string
          format: uri
          description:
            De URL waarmee de inhoud van het INFORMATIEOBJECT op te vragen
            is.
          maxLength: 200
        beschrijving:
          type: string
          description: Een generieke beschrijving van de inhoud van het INFORMATIEOBJECT.
          maxLength: 1000
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt.
        indicatieGebruiksrecht:
          type: boolean
          nullable: true
          description:
            Indicatie of er beperkingen gelden aangaande het gebruik van
            het informatieobject anders dan raadpleging. Dit veld mag `null` zijn
            om aan te geven dat de indicatie nog niet bekend is. Als de indicatie
            gezet is, dan kan je de gebruiksrechten die van toepassing zijn raadplegen
            via de GEBRUIKSRECHTen resource.
        verschijningsvorm:
          type: string
          description: De essentiële opmaakaspecten van een INFORMATIEOBJECT.
        ondertekening:
          allOf:
            - $ref: '#/components/schemas/Ondertekening'
          nullable: true
          description:
            Aanduiding van de rechtskracht van een informatieobject. Mag
            niet van een waarde zijn voorzien als de `status` de waarde 'in bewerking'
            of 'ter vaststelling' heeft.
        integriteit:
          allOf:
            - $ref: '#/components/schemas/Integriteit'
          nullable: true
          description:
            Uitdrukking van mate van volledigheid en onbeschadigd zijn
            van digitaal bestand.
        informatieobjecttype:
          type: string
          format: uri
          description:
            URL-referentie naar het INFORMATIEOBJECTTYPE (in de Catalogi
            API).
          maxLength: 200
        locked:
          type: boolean
          readOnly: true
          description:
            Geeft aan of het document gelocked is. Alleen als een document
            gelocked is, mogen er aanpassingen gemaakt worden.
        bestandsdelen:
          type: array
          items:
            $ref: '#/components/schemas/BestandsDeel'
          readOnly: true
        lock:
          type: string
          readOnly: true
          description:
            Lock id generated if the large file is created and should be
            used while updating the document. Documents with base64 encoded files
            are created without lock
      required:
        - auteur
        - beginRegistratie
        - bestandsdelen
        - bronorganisatie
        - creatiedatum
        - informatieobjecttype
        - lock
        - locked
        - taal
        - titel
        - url
        - versie
    EnkelvoudigInformatieObjectCreateLockRequest:
      type: object
      description: |-
        This serializer class is used by EnkelvoudigInformatieObjectViewSet for
        create operation for large files
      properties:
        identificatie:
          type: string
          description:
            Een binnen een gegeven context ondubbelzinnige referentie naar
            het INFORMATIEOBJECT.
          maxLength: 40
        bronorganisatie:
          type: string
          minLength: 1
          description:
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie
            die het informatieobject heeft gecreëerd of heeft ontvangen en als eerste
            in een samenwerkingsketen heeft vastgelegd.
          maxLength: 9
        creatiedatum:
          type: string
          format: date
          description: Een datum of een gebeurtenis in de levenscyclus van het INFORMATIEOBJECT.
        titel:
          type: string
          minLength: 1
          description: De naam waaronder het INFORMATIEOBJECT formeel bekend is.
          maxLength: 200
        vertrouwelijkheidaanduiding:
          description: |-
            Aanduiding van de mate waarin het INFORMATIEOBJECT voor de openbaarheid bestemd is.

            Uitleg bij mogelijke waarden:

            * `openbaar` - Openbaar
            * `beperkt_openbaar` - Beperkt openbaar
            * `intern` - Intern
            * `zaakvertrouwelijk` - Zaakvertrouwelijk
            * `vertrouwelijk` - Vertrouwelijk
            * `confidentieel` - Confidentieel
            * `geheim` - Geheim
            * `zeer_geheim` - Zeer geheim
          oneOf:
            - $ref: '#/components/schemas/VertrouwelijkheidaanduidingEnum'
            - $ref: '#/components/schemas/BlankEnum'
        auteur:
          type: string
          minLength: 1
          description:
            De persoon of organisatie die in de eerste plaats verantwoordelijk
            is voor het creëren van de inhoud van het INFORMATIEOBJECT.
          maxLength: 200
        status:
          description: |-
            Aanduiding van de stand van zaken van een INFORMATIEOBJECT. De waarden 'in bewerking' en 'ter vaststelling' komen niet voor als het attribuut `ontvangstdatum` van een waarde is voorzien. Wijziging van de Status in 'gearchiveerd' impliceert dat het informatieobject een duurzaam, niet-wijzigbaar Formaat dient te hebben.

            Uitleg bij mogelijke waarden:

            * `in_bewerking` - (In bewerking) Aan het informatieobject wordt nog gewerkt.
            * `ter_vaststelling` - (Ter vaststelling) Informatieobject gereed maar moet nog vastgesteld worden.
            * `definitief` - (Definitief) Informatieobject door bevoegd iets of iemand vastgesteld dan wel ontvangen.
            * `gearchiveerd` - (Gearchiveerd) Informatieobject duurzaam bewaarbaar gemaakt; een gearchiveerd informatie-element.
          oneOf:
            - $ref: '#/components/schemas/StatusEnum'
            - $ref: '#/components/schemas/BlankEnum'
        formaat:
          type: string
          description:
            'Het "Media Type" (voorheen "MIME type") voor de wijze waaropde
            inhoud van het INFORMATIEOBJECT is vastgelegd in een computerbestand.
            Voorbeeld: `application/msword`. Zie: https://www.iana.org/assignments/media-types/media-types.xhtml'
          maxLength: 255
        taal:
          type: string
          minLength: 3
          description:
            'Een ISO 639-2/B taalcode waarin de inhoud van het INFORMATIEOBJECT
            is vastgelegd. Voorbeeld: `nld`. Zie: https://www.iso.org/standard/4767.html'
          maxLength: 3
        bestandsnaam:
          type: string
          description:
            De naam van het fysieke bestand waarin de inhoud van het informatieobject
            is vastgelegd, inclusief extensie.
          maxLength: 255
        inhoud:
          type: string
          format: binary
          nullable: true
        bestandsomvang:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          description: Aantal bytes dat de inhoud van INFORMATIEOBJECT in beslag neemt.
        link:
          type: string
          format: uri
          description:
            De URL waarmee de inhoud van het INFORMATIEOBJECT op te vragen
            is.
          maxLength: 200
        beschrijving:
          type: string
          description: Een generieke beschrijving van de inhoud van het INFORMATIEOBJECT.
          maxLength: 1000
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt.
        indicatieGebruiksrecht:
          type: boolean
          nullable: true
          description:
            Indicatie of er beperkingen gelden aangaande het gebruik van
            het informatieobject anders dan raadpleging. Dit veld mag `null` zijn
            om aan te geven dat de indicatie nog niet bekend is. Als de indicatie
            gezet is, dan kan je de gebruiksrechten die van toepassing zijn raadplegen
            via de GEBRUIKSRECHTen resource.
        verschijningsvorm:
          type: string
          description: De essentiële opmaakaspecten van een INFORMATIEOBJECT.
        ondertekening:
          allOf:
            - $ref: '#/components/schemas/OndertekeningRequest'
          nullable: true
          description:
            Aanduiding van de rechtskracht van een informatieobject. Mag
            niet van een waarde zijn voorzien als de `status` de waarde 'in bewerking'
            of 'ter vaststelling' heeft.
        integriteit:
          allOf:
            - $ref: '#/components/schemas/IntegriteitRequest'
          nullable: true
          description:
            Uitdrukking van mate van volledigheid en onbeschadigd zijn
            van digitaal bestand.
        informatieobjecttype:
          type: string
          format: uri
          minLength: 1
          description:
            URL-referentie naar het INFORMATIEOBJECTTYPE (in de Catalogi
            API).
          maxLength: 200
      required:
        - auteur
        - bronorganisatie
        - creatiedatum
        - informatieobjecttype
        - taal
        - titel
    EnkelvoudigInformatieObjectRequest:
      type: object
      description: Serializer for the EnkelvoudigInformatieObject model
      properties:
        identificatie:
          type: string
          description:
            Een binnen een gegeven context ondubbelzinnige referentie naar
            het INFORMATIEOBJECT.
          maxLength: 40
        bronorganisatie:
          type: string
          minLength: 1
          description:
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie
            die het informatieobject heeft gecreëerd of heeft ontvangen en als eerste
            in een samenwerkingsketen heeft vastgelegd.
          maxLength: 9
        creatiedatum:
          type: string
          format: date
          description: Een datum of een gebeurtenis in de levenscyclus van het INFORMATIEOBJECT.
        titel:
          type: string
          minLength: 1
          description: De naam waaronder het INFORMATIEOBJECT formeel bekend is.
          maxLength: 200
        vertrouwelijkheidaanduiding:
          description: |-
            Aanduiding van de mate waarin het INFORMATIEOBJECT voor de openbaarheid bestemd is.

            Uitleg bij mogelijke waarden:

            * `openbaar` - Openbaar
            * `beperkt_openbaar` - Beperkt openbaar
            * `intern` - Intern
            * `zaakvertrouwelijk` - Zaakvertrouwelijk
            * `vertrouwelijk` - Vertrouwelijk
            * `confidentieel` - Confidentieel
            * `geheim` - Geheim
            * `zeer_geheim` - Zeer geheim
          oneOf:
            - $ref: '#/components/schemas/VertrouwelijkheidaanduidingEnum'
            - $ref: '#/components/schemas/BlankEnum'
        auteur:
          type: string
          minLength: 1
          description:
            De persoon of organisatie die in de eerste plaats verantwoordelijk
            is voor het creëren van de inhoud van het INFORMATIEOBJECT.
          maxLength: 200
        status:
          description: |-
            Aanduiding van de stand van zaken van een INFORMATIEOBJECT. De waarden 'in bewerking' en 'ter vaststelling' komen niet voor als het attribuut `ontvangstdatum` van een waarde is voorzien. Wijziging van de Status in 'gearchiveerd' impliceert dat het informatieobject een duurzaam, niet-wijzigbaar Formaat dient te hebben.

            Uitleg bij mogelijke waarden:

            * `in_bewerking` - (In bewerking) Aan het informatieobject wordt nog gewerkt.
            * `ter_vaststelling` - (Ter vaststelling) Informatieobject gereed maar moet nog vastgesteld worden.
            * `definitief` - (Definitief) Informatieobject door bevoegd iets of iemand vastgesteld dan wel ontvangen.
            * `gearchiveerd` - (Gearchiveerd) Informatieobject duurzaam bewaarbaar gemaakt; een gearchiveerd informatie-element.
          oneOf:
            - $ref: '#/components/schemas/StatusEnum'
            - $ref: '#/components/schemas/BlankEnum'
        formaat:
          type: string
          description:
            'Het "Media Type" (voorheen "MIME type") voor de wijze waaropde
            inhoud van het INFORMATIEOBJECT is vastgelegd in een computerbestand.
            Voorbeeld: `application/msword`. Zie: https://www.iana.org/assignments/media-types/media-types.xhtml'
          maxLength: 255
        taal:
          type: string
          minLength: 3
          description:
            'Een ISO 639-2/B taalcode waarin de inhoud van het INFORMATIEOBJECT
            is vastgelegd. Voorbeeld: `nld`. Zie: https://www.iso.org/standard/4767.html'
          maxLength: 3
        bestandsnaam:
          type: string
          description:
            De naam van het fysieke bestand waarin de inhoud van het informatieobject
            is vastgelegd, inclusief extensie.
          maxLength: 255
        inhoud:
          type: string
          format: binary
          nullable: true
        bestandsomvang:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          description: Aantal bytes dat de inhoud van INFORMATIEOBJECT in beslag neemt.
        link:
          type: string
          format: uri
          description:
            De URL waarmee de inhoud van het INFORMATIEOBJECT op te vragen
            is.
          maxLength: 200
        beschrijving:
          type: string
          description: Een generieke beschrijving van de inhoud van het INFORMATIEOBJECT.
          maxLength: 1000
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt.
        indicatieGebruiksrecht:
          type: boolean
          nullable: true
          description:
            Indicatie of er beperkingen gelden aangaande het gebruik van
            het informatieobject anders dan raadpleging. Dit veld mag `null` zijn
            om aan te geven dat de indicatie nog niet bekend is. Als de indicatie
            gezet is, dan kan je de gebruiksrechten die van toepassing zijn raadplegen
            via de GEBRUIKSRECHTen resource.
        verschijningsvorm:
          type: string
          description: De essentiële opmaakaspecten van een INFORMATIEOBJECT.
        ondertekening:
          allOf:
            - $ref: '#/components/schemas/OndertekeningRequest'
          nullable: true
          description:
            Aanduiding van de rechtskracht van een informatieobject. Mag
            niet van een waarde zijn voorzien als de `status` de waarde 'in bewerking'
            of 'ter vaststelling' heeft.
        integriteit:
          allOf:
            - $ref: '#/components/schemas/IntegriteitRequest'
          nullable: true
          description:
            Uitdrukking van mate van volledigheid en onbeschadigd zijn
            van digitaal bestand.
        informatieobjecttype:
          type: string
          format: uri
          minLength: 1
          description:
            URL-referentie naar het INFORMATIEOBJECTTYPE (in de Catalogi
            API).
          maxLength: 200
      required:
        - auteur
        - bronorganisatie
        - creatiedatum
        - informatieobjecttype
        - taal
        - titel
    EnkelvoudigInformatieObjectWithLock:
      type: object
      description: |-
        This serializer class is used by EnkelvoudigInformatieObjectViewSet for
        update and partial_update operations
      properties:
        url:
          type: string
          format: uri
          readOnly: true
          description:
            URL-referentie naar dit object. Dit is de unieke identificatie
            en locatie van dit object.
          minLength: 1
          maxLength: 1000
        identificatie:
          type: string
          description:
            Een binnen een gegeven context ondubbelzinnige referentie naar
            het INFORMATIEOBJECT.
          maxLength: 40
        bronorganisatie:
          type: string
          description:
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie
            die het informatieobject heeft gecreëerd of heeft ontvangen en als eerste
            in een samenwerkingsketen heeft vastgelegd.
          maxLength: 9
        creatiedatum:
          type: string
          format: date
          description: Een datum of een gebeurtenis in de levenscyclus van het INFORMATIEOBJECT.
        titel:
          type: string
          description: De naam waaronder het INFORMATIEOBJECT formeel bekend is.
          maxLength: 200
        vertrouwelijkheidaanduiding:
          description: |-
            Aanduiding van de mate waarin het INFORMATIEOBJECT voor de openbaarheid bestemd is.

            Uitleg bij mogelijke waarden:

            * `openbaar` - Openbaar
            * `beperkt_openbaar` - Beperkt openbaar
            * `intern` - Intern
            * `zaakvertrouwelijk` - Zaakvertrouwelijk
            * `vertrouwelijk` - Vertrouwelijk
            * `confidentieel` - Confidentieel
            * `geheim` - Geheim
            * `zeer_geheim` - Zeer geheim
          oneOf:
            - $ref: '#/components/schemas/VertrouwelijkheidaanduidingEnum'
            - $ref: '#/components/schemas/BlankEnum'
        auteur:
          type: string
          description:
            De persoon of organisatie die in de eerste plaats verantwoordelijk
            is voor het creëren van de inhoud van het INFORMATIEOBJECT.
          maxLength: 200
        status:
          description: |-
            Aanduiding van de stand van zaken van een INFORMATIEOBJECT. De waarden 'in bewerking' en 'ter vaststelling' komen niet voor als het attribuut `ontvangstdatum` van een waarde is voorzien. Wijziging van de Status in 'gearchiveerd' impliceert dat het informatieobject een duurzaam, niet-wijzigbaar Formaat dient te hebben.

            Uitleg bij mogelijke waarden:

            * `in_bewerking` - (In bewerking) Aan het informatieobject wordt nog gewerkt.
            * `ter_vaststelling` - (Ter vaststelling) Informatieobject gereed maar moet nog vastgesteld worden.
            * `definitief` - (Definitief) Informatieobject door bevoegd iets of iemand vastgesteld dan wel ontvangen.
            * `gearchiveerd` - (Gearchiveerd) Informatieobject duurzaam bewaarbaar gemaakt; een gearchiveerd informatie-element.
          oneOf:
            - $ref: '#/components/schemas/StatusEnum'
            - $ref: '#/components/schemas/BlankEnum'
        formaat:
          type: string
          description:
            'Het "Media Type" (voorheen "MIME type") voor de wijze waaropde
            inhoud van het INFORMATIEOBJECT is vastgelegd in een computerbestand.
            Voorbeeld: `application/msword`. Zie: https://www.iana.org/assignments/media-types/media-types.xhtml'
          maxLength: 255
        taal:
          type: string
          description:
            'Een ISO 639-2/B taalcode waarin de inhoud van het INFORMATIEOBJECT
            is vastgelegd. Voorbeeld: `nld`. Zie: https://www.iso.org/standard/4767.html'
          maxLength: 3
          minLength: 3
        versie:
          type: integer
          readOnly: true
          description:
            Het (automatische) versienummer van het INFORMATIEOBJECT. Deze
            begint bij 1 als het INFORMATIEOBJECT aangemaakt wordt.
        beginRegistratie:
          type: string
          format: date-time
          readOnly: true
          description:
            Een datumtijd in ISO8601 formaat waarop deze versie van het
            INFORMATIEOBJECT is aangemaakt of gewijzigd.
        bestandsnaam:
          type: string
          description:
            De naam van het fysieke bestand waarin de inhoud van het informatieobject
            is vastgelegd, inclusief extensie.
          maxLength: 255
        inhoud:
          type: string
          format: uri
          nullable: true
        bestandsomvang:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          description: Aantal bytes dat de inhoud van INFORMATIEOBJECT in beslag neemt.
        link:
          type: string
          format: uri
          description:
            De URL waarmee de inhoud van het INFORMATIEOBJECT op te vragen
            is.
          maxLength: 200
        beschrijving:
          type: string
          description: Een generieke beschrijving van de inhoud van het INFORMATIEOBJECT.
          maxLength: 1000
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt.
        indicatieGebruiksrecht:
          type: boolean
          nullable: true
          description:
            Indicatie of er beperkingen gelden aangaande het gebruik van
            het informatieobject anders dan raadpleging. Dit veld mag `null` zijn
            om aan te geven dat de indicatie nog niet bekend is. Als de indicatie
            gezet is, dan kan je de gebruiksrechten die van toepassing zijn raadplegen
            via de GEBRUIKSRECHTen resource.
        verschijningsvorm:
          type: string
          description: De essentiële opmaakaspecten van een INFORMATIEOBJECT.
        ondertekening:
          allOf:
            - $ref: '#/components/schemas/Ondertekening'
          nullable: true
          description:
            Aanduiding van de rechtskracht van een informatieobject. Mag
            niet van een waarde zijn voorzien als de `status` de waarde 'in bewerking'
            of 'ter vaststelling' heeft.
        integriteit:
          allOf:
            - $ref: '#/components/schemas/Integriteit'
          nullable: true
          description:
            Uitdrukking van mate van volledigheid en onbeschadigd zijn
            van digitaal bestand.
        informatieobjecttype:
          type: string
          format: uri
          description:
            URL-referentie naar het INFORMATIEOBJECTTYPE (in de Catalogi
            API).
          maxLength: 200
        locked:
          type: boolean
          readOnly: true
          description:
            Geeft aan of het document gelocked is. Alleen als een document
            gelocked is, mogen er aanpassingen gemaakt worden.
        bestandsdelen:
          type: array
          items:
            $ref: '#/components/schemas/BestandsDeel'
          readOnly: true
      required:
        - auteur
        - beginRegistratie
        - bestandsdelen
        - bronorganisatie
        - creatiedatum
        - informatieobjecttype
        - locked
        - taal
        - titel
        - url
        - versie
    EnkelvoudigInformatieObjectWithLockRequest:
      type: object
      description: |-
        This serializer class is used by EnkelvoudigInformatieObjectViewSet for
        update and partial_update operations
      properties:
        identificatie:
          type: string
          description:
            Een binnen een gegeven context ondubbelzinnige referentie naar
            het INFORMATIEOBJECT.
          maxLength: 40
        bronorganisatie:
          type: string
          minLength: 1
          description:
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie
            die het informatieobject heeft gecreëerd of heeft ontvangen en als eerste
            in een samenwerkingsketen heeft vastgelegd.
          maxLength: 9
        creatiedatum:
          type: string
          format: date
          description: Een datum of een gebeurtenis in de levenscyclus van het INFORMATIEOBJECT.
        titel:
          type: string
          minLength: 1
          description: De naam waaronder het INFORMATIEOBJECT formeel bekend is.
          maxLength: 200
        vertrouwelijkheidaanduiding:
          description: |-
            Aanduiding van de mate waarin het INFORMATIEOBJECT voor de openbaarheid bestemd is.

            Uitleg bij mogelijke waarden:

            * `openbaar` - Openbaar
            * `beperkt_openbaar` - Beperkt openbaar
            * `intern` - Intern
            * `zaakvertrouwelijk` - Zaakvertrouwelijk
            * `vertrouwelijk` - Vertrouwelijk
            * `confidentieel` - Confidentieel
            * `geheim` - Geheim
            * `zeer_geheim` - Zeer geheim
          oneOf:
            - $ref: '#/components/schemas/VertrouwelijkheidaanduidingEnum'
            - $ref: '#/components/schemas/BlankEnum'
        auteur:
          type: string
          minLength: 1
          description:
            De persoon of organisatie die in de eerste plaats verantwoordelijk
            is voor het creëren van de inhoud van het INFORMATIEOBJECT.
          maxLength: 200
        status:
          description: |-
            Aanduiding van de stand van zaken van een INFORMATIEOBJECT. De waarden 'in bewerking' en 'ter vaststelling' komen niet voor als het attribuut `ontvangstdatum` van een waarde is voorzien. Wijziging van de Status in 'gearchiveerd' impliceert dat het informatieobject een duurzaam, niet-wijzigbaar Formaat dient te hebben.

            Uitleg bij mogelijke waarden:

            * `in_bewerking` - (In bewerking) Aan het informatieobject wordt nog gewerkt.
            * `ter_vaststelling` - (Ter vaststelling) Informatieobject gereed maar moet nog vastgesteld worden.
            * `definitief` - (Definitief) Informatieobject door bevoegd iets of iemand vastgesteld dan wel ontvangen.
            * `gearchiveerd` - (Gearchiveerd) Informatieobject duurzaam bewaarbaar gemaakt; een gearchiveerd informatie-element.
          oneOf:
            - $ref: '#/components/schemas/StatusEnum'
            - $ref: '#/components/schemas/BlankEnum'
        formaat:
          type: string
          description:
            'Het "Media Type" (voorheen "MIME type") voor de wijze waaropde
            inhoud van het INFORMATIEOBJECT is vastgelegd in een computerbestand.
            Voorbeeld: `application/msword`. Zie: https://www.iana.org/assignments/media-types/media-types.xhtml'
          maxLength: 255
        taal:
          type: string
          minLength: 3
          description:
            'Een ISO 639-2/B taalcode waarin de inhoud van het INFORMATIEOBJECT
            is vastgelegd. Voorbeeld: `nld`. Zie: https://www.iso.org/standard/4767.html'
          maxLength: 3
        bestandsnaam:
          type: string
          description:
            De naam van het fysieke bestand waarin de inhoud van het informatieobject
            is vastgelegd, inclusief extensie.
          maxLength: 255
        inhoud:
          type: string
          format: binary
          nullable: true
        bestandsomvang:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          description: Aantal bytes dat de inhoud van INFORMATIEOBJECT in beslag neemt.
        link:
          type: string
          format: uri
          description:
            De URL waarmee de inhoud van het INFORMATIEOBJECT op te vragen
            is.
          maxLength: 200
        beschrijving:
          type: string
          description: Een generieke beschrijving van de inhoud van het INFORMATIEOBJECT.
          maxLength: 1000
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt.
        indicatieGebruiksrecht:
          type: boolean
          nullable: true
          description:
            Indicatie of er beperkingen gelden aangaande het gebruik van
            het informatieobject anders dan raadpleging. Dit veld mag `null` zijn
            om aan te geven dat de indicatie nog niet bekend is. Als de indicatie
            gezet is, dan kan je de gebruiksrechten die van toepassing zijn raadplegen
            via de GEBRUIKSRECHTen resource.
        verschijningsvorm:
          type: string
          description: De essentiële opmaakaspecten van een INFORMATIEOBJECT.
        ondertekening:
          allOf:
            - $ref: '#/components/schemas/OndertekeningRequest'
          nullable: true
          description:
            Aanduiding van de rechtskracht van een informatieobject. Mag
            niet van een waarde zijn voorzien als de `status` de waarde 'in bewerking'
            of 'ter vaststelling' heeft.
        integriteit:
          allOf:
            - $ref: '#/components/schemas/IntegriteitRequest'
          nullable: true
          description:
            Uitdrukking van mate van volledigheid en onbeschadigd zijn
            van digitaal bestand.
        informatieobjecttype:
          type: string
          format: uri
          minLength: 1
          description:
            URL-referentie naar het INFORMATIEOBJECTTYPE (in de Catalogi
            API).
          maxLength: 200
        lock:
          type: string
          writeOnly: true
          minLength: 1
          description:
            Tijdens het updaten van een document (PATCH, PUT) moet het
            `lock` veld opgegeven worden. Bij het aanmaken (POST) mag het geen waarde
            hebben.
      required:
        - auteur
        - bronorganisatie
        - creatiedatum
        - informatieobjecttype
        - lock
        - taal
        - titel
    FieldValidationError:
      type: object
      description: Formaat van validatiefouten.
      properties:
        name:
          type: string
          description: Naam van het veld met ongeldige gegevens
        code:
          type: string
          description: Systeemcode die het type fout aangeeft
        reason:
          type: string
          description: Uitleg wat er precies fout is met de gegevens
      required:
        - code
        - name
        - reason
    Fout:
      type: object
      description: Formaat van HTTP 4xx en 5xx fouten.
      properties:
        type:
          type: string
          description: URI referentie naar het type fout, bedoeld voor developers
        code:
          type: string
          description: Systeemcode die het type fout aangeeft
        title:
          type: string
          description: Generieke titel voor het type fout
        status:
          type: integer
          description: De HTTP status code
        detail:
          type: string
          description: Extra informatie bij de fout, indien beschikbaar
        instance:
          type: string
          description:
            URI met referentie naar dit specifiek voorkomen van de fout.
            Deze kan gebruikt worden in combinatie met server logs, bijvoorbeeld.
      required:
        - code
        - detail
        - instance
        - status
        - title
    Gebruiksrechten:
      type: object
      properties:
        url:
          type: string
          format: uri
          readOnly: true
          description:
            URL-referentie naar dit object. Dit is de unieke identificatie
            en locatie van dit object.
          minLength: 1
          maxLength: 1000
        informatieobject:
          type: string
          format: uri
          description: URL-referentie naar het INFORMATIEOBJECT.
        startdatum:
          type: string
          format: date-time
          description:
            Begindatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn. Doorgaans is de datum van creatie van het informatieobject
            de startdatum.
        einddatum:
          type: string
          format: date-time
          nullable: true
          title: Startdatum
          description:
            Einddatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn.
        omschrijvingVoorwaarden:
          type: string
          description:
            Omschrijving van de van toepassing zijnde voorwaarden aan het
            gebruik anders dan raadpleging
      required:
        - informatieobject
        - omschrijvingVoorwaarden
        - startdatum
        - url
    GebruiksrechtenRequest:
      type: object
      properties:
        informatieobject:
          type: string
          format: uri
          description: URL-referentie naar het INFORMATIEOBJECT.
        startdatum:
          type: string
          format: date-time
          description:
            Begindatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn. Doorgaans is de datum van creatie van het informatieobject
            de startdatum.
        einddatum:
          type: string
          format: date-time
          nullable: true
          title: Startdatum
          description:
            Einddatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn.
        omschrijvingVoorwaarden:
          type: string
          minLength: 1
          description:
            Omschrijving van de van toepassing zijnde voorwaarden aan het
            gebruik anders dan raadpleging
      required:
        - informatieobject
        - omschrijvingVoorwaarden
        - startdatum
    Integriteit:
      type: object
      description:
        Uitdrukking van mate van volledigheid en onbeschadigd zijn van
        digitaal bestand.
      properties:
        algoritme:
          allOf:
            - $ref: '#/components/schemas/AlgoritmeEnum'
          description: |-
            Aanduiding van algoritme, gebruikt om de checksum te maken.

            Uitleg bij mogelijke waarden:

            * `crc_16` - CRC-16
            * `crc_32` - CRC-32
            * `crc_64` - CRC-64
            * `fletcher_4` - Fletcher-4
            * `fletcher_8` - Fletcher-8
            * `fletcher_16` - Fletcher-16
            * `fletcher_32` - Fletcher-32
            * `hmac` - HMAC
            * `md5` - MD5
            * `sha_1` - SHA-1
            * `sha_256` - SHA-256
            * `sha_512` - SHA-512
            * `sha_3` - SHA-3
        waarde:
          type: string
          description: De waarde van de checksum.
          maxLength: 128
        datum:
          type: string
          format: date
          description: Datum waarop de checksum is gemaakt.
      required:
        - algoritme
        - datum
        - waarde
      nullable: true
    IntegriteitRequest:
      type: object
      description:
        Uitdrukking van mate van volledigheid en onbeschadigd zijn van
        digitaal bestand.
      properties:
        algoritme:
          allOf:
            - $ref: '#/components/schemas/AlgoritmeEnum'
          description: |-
            Aanduiding van algoritme, gebruikt om de checksum te maken.

            Uitleg bij mogelijke waarden:

            * `crc_16` - CRC-16
            * `crc_32` - CRC-32
            * `crc_64` - CRC-64
            * `fletcher_4` - Fletcher-4
            * `fletcher_8` - Fletcher-8
            * `fletcher_16` - Fletcher-16
            * `fletcher_32` - Fletcher-32
            * `hmac` - HMAC
            * `md5` - MD5
            * `sha_1` - SHA-1
            * `sha_256` - SHA-256
            * `sha_512` - SHA-512
            * `sha_3` - SHA-3
        waarde:
          type: string
          minLength: 1
          description: De waarde van de checksum.
          maxLength: 128
        datum:
          type: string
          format: date
          description: Datum waarop de checksum is gemaakt.
      required:
        - algoritme
        - datum
        - waarde
      nullable: true
    ObjectInformatieObject:
      type: object
      properties:
        url:
          type: string
          format: uri
          readOnly: true
          description:
            URL-referentie naar dit object. Dit is de unieke identificatie
            en locatie van dit object.
          minLength: 1
          maxLength: 1000
        informatieobject:
          type: string
          format: uri
          description: URL-referentie naar het INFORMATIEOBJECT.
        object:
          type: string
          format: uri
          description:
            URL-referentie naar het gerelateerde OBJECT (in deze of een
            andere API).
          maxLength: 1000
        objectType:
          allOf:
            - $ref: '#/components/schemas/ObjectTypeEnum'
          title: Objecttype
          description: |-
            Het type van het gerelateerde OBJECT.

            Uitleg bij mogelijke waarden:

            * `besluit` - Besluit
            * `zaak` - Zaak
      required:
        - informatieobject
        - object
        - objectType
        - url
    ObjectInformatieObjectRequest:
      type: object
      properties:
        informatieobject:
          type: string
          format: uri
          description: URL-referentie naar het INFORMATIEOBJECT.
        object:
          type: string
          format: uri
          minLength: 1
          description:
            URL-referentie naar het gerelateerde OBJECT (in deze of een
            andere API).
          maxLength: 1000
        objectType:
          allOf:
            - $ref: '#/components/schemas/ObjectTypeEnum'
          title: Objecttype
          description: |-
            Het type van het gerelateerde OBJECT.

            Uitleg bij mogelijke waarden:

            * `besluit` - Besluit
            * `zaak` - Zaak
      required:
        - informatieobject
        - object
        - objectType
    ObjectTypeEnum:
      enum:
        - besluit
        - zaak
        - verzoek
      type: string
    Ondertekening:
      type: object
      description:
        Aanduiding van de rechtskracht van een informatieobject. Mag niet
        van een waarde zijn voorzien als de `status` de waarde 'in bewerking' of 'ter
        vaststelling' heeft.
      properties:
        soort:
          allOf:
            - $ref: '#/components/schemas/SoortEnum'
          title: Ondertekeningsoort
          description: |-
            Aanduiding van de wijze van ondertekening van het INFORMATIEOBJECT

            Uitleg bij mogelijke waarden:

            * `analoog` - Analoog
            * `digitaal` - Digitaal
            * `pki` - PKI
        datum:
          type: string
          format: date
          title: Ondertekeningdatum
          description:
            De datum waarop de ondertekening van het INFORMATIEOBJECT heeft
            plaatsgevonden.
      required:
        - datum
        - soort
      nullable: true
    OndertekeningRequest:
      type: object
      description:
        Aanduiding van de rechtskracht van een informatieobject. Mag niet
        van een waarde zijn voorzien als de `status` de waarde 'in bewerking' of 'ter
        vaststelling' heeft.
      properties:
        soort:
          allOf:
            - $ref: '#/components/schemas/SoortEnum'
          title: Ondertekeningsoort
          description: |-
            Aanduiding van de wijze van ondertekening van het INFORMATIEOBJECT

            Uitleg bij mogelijke waarden:

            * `analoog` - Analoog
            * `digitaal` - Digitaal
            * `pki` - PKI
        datum:
          type: string
          format: date
          title: Ondertekeningdatum
          description:
            De datum waarop de ondertekening van het INFORMATIEOBJECT heeft
            plaatsgevonden.
      required:
        - datum
        - soort
      nullable: true
    PaginatedEnkelvoudigInformatieObjectList:
      type: object
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/EnkelvoudigInformatieObject'
    PaginatedVerzendingList:
      type: object
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/Verzending'
    PatchedEnkelvoudigInformatieObjectWithLockRequest:
      type: object
      description: |-
        This serializer class is used by EnkelvoudigInformatieObjectViewSet for
        update and partial_update operations
      properties:
        identificatie:
          type: string
          description:
            Een binnen een gegeven context ondubbelzinnige referentie naar
            het INFORMATIEOBJECT.
          maxLength: 40
        bronorganisatie:
          type: string
          minLength: 1
          description:
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie
            die het informatieobject heeft gecreëerd of heeft ontvangen en als eerste
            in een samenwerkingsketen heeft vastgelegd.
          maxLength: 9
        creatiedatum:
          type: string
          format: date
          description: Een datum of een gebeurtenis in de levenscyclus van het INFORMATIEOBJECT.
        titel:
          type: string
          minLength: 1
          description: De naam waaronder het INFORMATIEOBJECT formeel bekend is.
          maxLength: 200
        vertrouwelijkheidaanduiding:
          description: |-
            Aanduiding van de mate waarin het INFORMATIEOBJECT voor de openbaarheid bestemd is.

            Uitleg bij mogelijke waarden:

            * `openbaar` - Openbaar
            * `beperkt_openbaar` - Beperkt openbaar
            * `intern` - Intern
            * `zaakvertrouwelijk` - Zaakvertrouwelijk
            * `vertrouwelijk` - Vertrouwelijk
            * `confidentieel` - Confidentieel
            * `geheim` - Geheim
            * `zeer_geheim` - Zeer geheim
          oneOf:
            - $ref: '#/components/schemas/VertrouwelijkheidaanduidingEnum'
            - $ref: '#/components/schemas/BlankEnum'
        auteur:
          type: string
          minLength: 1
          description:
            De persoon of organisatie die in de eerste plaats verantwoordelijk
            is voor het creëren van de inhoud van het INFORMATIEOBJECT.
          maxLength: 200
        status:
          description: |-
            Aanduiding van de stand van zaken van een INFORMATIEOBJECT. De waarden 'in bewerking' en 'ter vaststelling' komen niet voor als het attribuut `ontvangstdatum` van een waarde is voorzien. Wijziging van de Status in 'gearchiveerd' impliceert dat het informatieobject een duurzaam, niet-wijzigbaar Formaat dient te hebben.

            Uitleg bij mogelijke waarden:

            * `in_bewerking` - (In bewerking) Aan het informatieobject wordt nog gewerkt.
            * `ter_vaststelling` - (Ter vaststelling) Informatieobject gereed maar moet nog vastgesteld worden.
            * `definitief` - (Definitief) Informatieobject door bevoegd iets of iemand vastgesteld dan wel ontvangen.
            * `gearchiveerd` - (Gearchiveerd) Informatieobject duurzaam bewaarbaar gemaakt; een gearchiveerd informatie-element.
          oneOf:
            - $ref: '#/components/schemas/StatusEnum'
            - $ref: '#/components/schemas/BlankEnum'
        formaat:
          type: string
          description:
            'Het "Media Type" (voorheen "MIME type") voor de wijze waaropde
            inhoud van het INFORMATIEOBJECT is vastgelegd in een computerbestand.
            Voorbeeld: `application/msword`. Zie: https://www.iana.org/assignments/media-types/media-types.xhtml'
          maxLength: 255
        taal:
          type: string
          minLength: 3
          description:
            'Een ISO 639-2/B taalcode waarin de inhoud van het INFORMATIEOBJECT
            is vastgelegd. Voorbeeld: `nld`. Zie: https://www.iso.org/standard/4767.html'
          maxLength: 3
        bestandsnaam:
          type: string
          description:
            De naam van het fysieke bestand waarin de inhoud van het informatieobject
            is vastgelegd, inclusief extensie.
          maxLength: 255
        inhoud:
          type: string
          format: binary
          nullable: true
        bestandsomvang:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          description: Aantal bytes dat de inhoud van INFORMATIEOBJECT in beslag neemt.
        link:
          type: string
          format: uri
          description:
            De URL waarmee de inhoud van het INFORMATIEOBJECT op te vragen
            is.
          maxLength: 200
        beschrijving:
          type: string
          description: Een generieke beschrijving van de inhoud van het INFORMATIEOBJECT.
          maxLength: 1000
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt.
        indicatieGebruiksrecht:
          type: boolean
          nullable: true
          description:
            Indicatie of er beperkingen gelden aangaande het gebruik van
            het informatieobject anders dan raadpleging. Dit veld mag `null` zijn
            om aan te geven dat de indicatie nog niet bekend is. Als de indicatie
            gezet is, dan kan je de gebruiksrechten die van toepassing zijn raadplegen
            via de GEBRUIKSRECHTen resource.
        verschijningsvorm:
          type: string
          description: De essentiële opmaakaspecten van een INFORMATIEOBJECT.
        ondertekening:
          allOf:
            - $ref: '#/components/schemas/OndertekeningRequest'
          nullable: true
          description:
            Aanduiding van de rechtskracht van een informatieobject. Mag
            niet van een waarde zijn voorzien als de `status` de waarde 'in bewerking'
            of 'ter vaststelling' heeft.
        integriteit:
          allOf:
            - $ref: '#/components/schemas/IntegriteitRequest'
          nullable: true
          description:
            Uitdrukking van mate van volledigheid en onbeschadigd zijn
            van digitaal bestand.
        informatieobjecttype:
          type: string
          format: uri
          minLength: 1
          description:
            URL-referentie naar het INFORMATIEOBJECTTYPE (in de Catalogi
            API).
          maxLength: 200
        lock:
          type: string
          writeOnly: true
          minLength: 1
          description:
            Tijdens het updaten van een document (PATCH, PUT) moet het
            `lock` veld opgegeven worden. Bij het aanmaken (POST) mag het geen waarde
            hebben.
    PatchedGebruiksrechtenRequest:
      type: object
      properties:
        informatieobject:
          type: string
          format: uri
          description: URL-referentie naar het INFORMATIEOBJECT.
        startdatum:
          type: string
          format: date-time
          description:
            Begindatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn. Doorgaans is de datum van creatie van het informatieobject
            de startdatum.
        einddatum:
          type: string
          format: date-time
          nullable: true
          title: Startdatum
          description:
            Einddatum van de periode waarin de gebruiksrechtvoorwaarden
            van toepassing zijn.
        omschrijvingVoorwaarden:
          type: string
          minLength: 1
          description:
            Omschrijving van de van toepassing zijnde voorwaarden aan het
            gebruik anders dan raadpleging
    PatchedVerzendingRequest:
      type: object
      description: |-
        Set gegevensgroepdata from validated nested data.

        Usage: include the mixin on the ModelSerializer that has gegevensgroepen.
      properties:
        betrokkene:
          type: string
          format: uri
          minLength: 1
          description:
            URL-referentie naar de betrokkene waarvan het informatieobject
            is ontvangen of waaraan dit is verzonden.
          maxLength: 200
        informatieobject:
          type: string
          format: uri
          description:
            URL-referentie naar het informatieobject dat is ontvangen of
            verzonden.
        aardRelatie:
          allOf:
            - $ref: '#/components/schemas/AardRelatieEnum'
          description:
            Omschrijving van de aard van de relatie van de BETROKKENE tot
            het INFORMATIEOBJECT.
        toelichting:
          type: string
          description: Verduidelijking van de afzender- of geadresseerde-relatie.
          maxLength: 200
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt. Vervangt het gelijknamige attribuut
            uit Informatieobject. Verplicht gevuld wanneer aardRelatie de waarde 'afzender'
            heeft.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt. Vervangt het gelijknamige attribuut
            uit Informatieobject. Verplicht gevuld wanneer aardRelatie de waarde 'geadresseerde'
            heeft.
        contactPersoon:
          type: string
          format: uri
          minLength: 1
          title: Contactpersoon
          description:
            URL-referentie naar de persoon die als aanspreekpunt fungeert
            voor de BETROKKENE inzake het ontvangen of verzonden INFORMATIEOBJECT.
          maxLength: 1000
        contactpersoonnaam:
          type: string
          description:
            De opgemaakte naam van de persoon die als aanspreekpunt fungeert
            voorde BETROKKENE inzake het ontvangen of verzonden INFORMATIEOBJECT.
          maxLength: 40
        binnenlandsCorrespondentieadres:
          allOf:
            - $ref: '#/components/schemas/BinnenlandsCorrespondentieadresVerzendingRequest'
          nullable: true
          description:
            Het correspondentieadres, betreffende een adresseerbaar object,
            van de BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld in
            het ontvangen of verzonden INFORMATIEOBJECT indien dat afwijkt van het
            reguliere binnenlandse correspondentieadres van BETROKKENE.
        buitenlandsCorrespondentieadres:
          allOf:
            - $ref: '#/components/schemas/BuitenlandsCorrespondentieadresVerzendingRequest'
          nullable: true
          description:
            De gegevens van het adres in het buitenland van BETROKKENE,
            zijnde afzender of geadresseerde, zoals vermeld in het ontvangen of verzonden
            INFORMATIEOBJECT en dat afwijkt van de reguliere correspondentiegegevens
            van BETROKKENE.
        correspondentiePostadres:
          allOf:
            - $ref: '#/components/schemas/BuitenlandsCorrespondentiepostadresVerzendingRequest'
          nullable: true
          description:
            De gegevens die tezamen een postbusadres of antwoordnummeradres
            vormen van BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld
            in het ontvangen of verzonden INFORMATIEOBJECT en dat afwijkt van de reguliere
            correspondentiegegevens van BETROKKENE.
    PostadresTypeEnum:
      enum:
        - antwoordnummer
        - postbusnummer
      type: string
    SoortEnum:
      enum:
        - analoog
        - digitaal
        - pki
      type: string
    StatusEnum:
      enum:
        - in_bewerking
        - ter_vaststelling
        - definitief
        - gearchiveerd
      type: string
    ValidatieFout:
      type: object
      description: Formaat van HTTP 4xx en 5xx fouten.
      properties:
        type:
          type: string
          description: URI referentie naar het type fout, bedoeld voor developers
        code:
          type: string
          description: Systeemcode die het type fout aangeeft
        title:
          type: string
          description: Generieke titel voor het type fout
        status:
          type: integer
          description: De HTTP status code
        detail:
          type: string
          description: Extra informatie bij de fout, indien beschikbaar
        instance:
          type: string
          description:
            URI met referentie naar dit specifiek voorkomen van de fout.
            Deze kan gebruikt worden in combinatie met server logs, bijvoorbeeld.
        invalidParams:
          type: array
          items:
            $ref: '#/components/schemas/FieldValidationError'
      required:
        - code
        - detail
        - instance
        - invalidParams
        - status
        - title
    VertrouwelijkheidaanduidingEnum:
      enum:
        - openbaar
        - beperkt_openbaar
        - intern
        - zaakvertrouwelijk
        - vertrouwelijk
        - confidentieel
        - geheim
        - zeer_geheim
      type: string
    Verzending:
      type: object
      description: |-
        Set gegevensgroepdata from validated nested data.

        Usage: include the mixin on the ModelSerializer that has gegevensgroepen.
      properties:
        url:
          type: string
          format: uri
          readOnly: true
          description:
            URL-referentie naar dit object. Dit is de unieke identificatie
            en locatie van dit object.
          minLength: 1
          maxLength: 1000
        betrokkene:
          type: string
          format: uri
          description:
            URL-referentie naar de betrokkene waarvan het informatieobject
            is ontvangen of waaraan dit is verzonden.
          maxLength: 200
        informatieobject:
          type: string
          format: uri
          description:
            URL-referentie naar het informatieobject dat is ontvangen of
            verzonden.
        aardRelatie:
          allOf:
            - $ref: '#/components/schemas/AardRelatieEnum'
          description:
            Omschrijving van de aard van de relatie van de BETROKKENE tot
            het INFORMATIEOBJECT.
        toelichting:
          type: string
          description: Verduidelijking van de afzender- of geadresseerde-relatie.
          maxLength: 200
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt. Vervangt het gelijknamige attribuut
            uit Informatieobject. Verplicht gevuld wanneer aardRelatie de waarde 'afzender'
            heeft.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt. Vervangt het gelijknamige attribuut
            uit Informatieobject. Verplicht gevuld wanneer aardRelatie de waarde 'geadresseerde'
            heeft.
        contactPersoon:
          type: string
          format: uri
          title: Contactpersoon
          description:
            URL-referentie naar de persoon die als aanspreekpunt fungeert
            voor de BETROKKENE inzake het ontvangen of verzonden INFORMATIEOBJECT.
          maxLength: 1000
        contactpersoonnaam:
          type: string
          description:
            De opgemaakte naam van de persoon die als aanspreekpunt fungeert
            voorde BETROKKENE inzake het ontvangen of verzonden INFORMATIEOBJECT.
          maxLength: 40
        binnenlandsCorrespondentieadres:
          allOf:
            - $ref: '#/components/schemas/BinnenlandsCorrespondentieadresVerzending'
          nullable: true
          description:
            Het correspondentieadres, betreffende een adresseerbaar object,
            van de BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld in
            het ontvangen of verzonden INFORMATIEOBJECT indien dat afwijkt van het
            reguliere binnenlandse correspondentieadres van BETROKKENE.
        buitenlandsCorrespondentieadres:
          allOf:
            - $ref: '#/components/schemas/BuitenlandsCorrespondentieadresVerzending'
          nullable: true
          description:
            De gegevens van het adres in het buitenland van BETROKKENE,
            zijnde afzender of geadresseerde, zoals vermeld in het ontvangen of verzonden
            INFORMATIEOBJECT en dat afwijkt van de reguliere correspondentiegegevens
            van BETROKKENE.
        correspondentiePostadres:
          allOf:
            - $ref: '#/components/schemas/BuitenlandsCorrespondentiepostadresVerzending'
          nullable: true
          description:
            De gegevens die tezamen een postbusadres of antwoordnummeradres
            vormen van BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld
            in het ontvangen of verzonden INFORMATIEOBJECT en dat afwijkt van de reguliere
            correspondentiegegevens van BETROKKENE.
      required:
        - aardRelatie
        - betrokkene
        - contactPersoon
        - informatieobject
        - url
    VerzendingRequest:
      type: object
      description: |-
        Set gegevensgroepdata from validated nested data.

        Usage: include the mixin on the ModelSerializer that has gegevensgroepen.
      properties:
        betrokkene:
          type: string
          format: uri
          minLength: 1
          description:
            URL-referentie naar de betrokkene waarvan het informatieobject
            is ontvangen of waaraan dit is verzonden.
          maxLength: 200
        informatieobject:
          type: string
          format: uri
          description:
            URL-referentie naar het informatieobject dat is ontvangen of
            verzonden.
        aardRelatie:
          allOf:
            - $ref: '#/components/schemas/AardRelatieEnum'
          description:
            Omschrijving van de aard van de relatie van de BETROKKENE tot
            het INFORMATIEOBJECT.
        toelichting:
          type: string
          description: Verduidelijking van de afzender- of geadresseerde-relatie.
          maxLength: 200
        ontvangstdatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT ontvangen is. Verplicht
            te registreren voor INFORMATIEOBJECTen die van buiten de zaakbehandelende
            organisatie(s) ontvangen zijn. Ontvangst en verzending is voorbehouden
            aan documenten die van of naar andere personen ontvangen of verzonden
            zijn waarbij die personen niet deel uit maken van de behandeling van de
            zaak waarin het document een rol speelt. Vervangt het gelijknamige attribuut
            uit Informatieobject. Verplicht gevuld wanneer aardRelatie de waarde 'afzender'
            heeft.
        verzenddatum:
          type: string
          format: date
          nullable: true
          description:
            De datum waarop het INFORMATIEOBJECT verzonden is, zoals deze
            op het INFORMATIEOBJECT vermeld is. Dit geldt voor zowel inkomende als
            uitgaande INFORMATIEOBJECTen. Eenzelfde informatieobject kan niet tegelijk
            inkomend en uitgaand zijn. Ontvangst en verzending is voorbehouden aan
            documenten die van of naar andere personen ontvangen of verzonden zijn
            waarbij die personen niet deel uit maken van de behandeling van de zaak
            waarin het document een rol speelt. Vervangt het gelijknamige attribuut
            uit Informatieobject. Verplicht gevuld wanneer aardRelatie de waarde 'geadresseerde'
            heeft.
        contactPersoon:
          type: string
          format: uri
          minLength: 1
          title: Contactpersoon
          description:
            URL-referentie naar de persoon die als aanspreekpunt fungeert
            voor de BETROKKENE inzake het ontvangen of verzonden INFORMATIEOBJECT.
          maxLength: 1000
        contactpersoonnaam:
          type: string
          description:
            De opgemaakte naam van de persoon die als aanspreekpunt fungeert
            voorde BETROKKENE inzake het ontvangen of verzonden INFORMATIEOBJECT.
          maxLength: 40
        binnenlandsCorrespondentieadres:
          allOf:
            - $ref: '#/components/schemas/BinnenlandsCorrespondentieadresVerzendingRequest'
          nullable: true
          description:
            Het correspondentieadres, betreffende een adresseerbaar object,
            van de BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld in
            het ontvangen of verzonden INFORMATIEOBJECT indien dat afwijkt van het
            reguliere binnenlandse correspondentieadres van BETROKKENE.
        buitenlandsCorrespondentieadres:
          allOf:
            - $ref: '#/components/schemas/BuitenlandsCorrespondentieadresVerzendingRequest'
          nullable: true
          description:
            De gegevens van het adres in het buitenland van BETROKKENE,
            zijnde afzender of geadresseerde, zoals vermeld in het ontvangen of verzonden
            INFORMATIEOBJECT en dat afwijkt van de reguliere correspondentiegegevens
            van BETROKKENE.
        correspondentiePostadres:
          allOf:
            - $ref: '#/components/schemas/BuitenlandsCorrespondentiepostadresVerzendingRequest'
          nullable: true
          description:
            De gegevens die tezamen een postbusadres of antwoordnummeradres
            vormen van BETROKKENE, zijnde afzender of geadresseerde, zoals vermeld
            in het ontvangen of verzonden INFORMATIEOBJECT en dat afwijkt van de reguliere
            correspondentiegegevens van BETROKKENE.
      required:
        - aardRelatie
        - betrokkene
        - contactPersoon
        - informatieobject
  securitySchemes:
    JWT-Claims:
      type: http
      bearerFormat: JWT
      scheme: bearer
servers:
  - url: /api/v1
